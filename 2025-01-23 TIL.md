# TIL: 인덱스

학습일자: 2025년 01월 23일

## 학습하는데 어떤 문제에 부딪혔는지

들어보기만 했던 인덱스에 대해 학습하게 되었다. 특히, 인덱스를 활용하지 못하게 하는 원인들을 알 수 있었다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

강의를 열심히 들었다.

## 어떻게 그 문제를 해결 했는지

열심히 정리를 했다.

## 오늘 내가 새롭게 배운 것은 무엇인지

### 인덱스를 활용하지 못하게 하는 주요 원인

- 인덱스 컬럼에 함수 또는 연산 사용 (→ 좌변 가공)
: 옵티마이저가 시작점을 찾을 수 없음. 오리지널 데이터를 찾아야하기 때문. 
따라서 형변환, 서브스트링, UPPER 같이 가공을 하면 타지 못하게 되버림.
- 데이터 타입 불일치
- 와일드카드(%) 사용 위치
언더바 _ 도 마찬가지.
- NULL 값 처리
- OR 조건이 포함된 경우

### 1. 인덱스 컬럼에 함수 또는 연산 사용

- WHERE 절에서 인덱스 컬럼에 함수나 연산을 적용하면 인덱스를 사용할 수 없음
    
    ```sql
    SELECT * FROM employees WHERE ROUND(salary) = 5000;
    >>
    SELECT * FROM employees WHERE salary BETWEEN 4999.5 AND 5000.5;
    ```
    

### 2. 데이터 타입 불일치

- WHERE 조건에서 인덱스 컬럼의 데이터 타입과 쿼리에 사용된 값의 데이터 타입이 다를 경우
    
    ```sql
    SELECT * FROM employees WHERE employee_id = '101';
    >>
    SELECT * FROM employees WHERE employee_id = 101;
    ```
    

### 3. 와일드카드(%) 사용 위치

- LIKE 조건에서 % 와일드카드가 문자열의 앞부분에 사용될 경우
    
    ```sql
    SELECT * FROM customers WHERE name LIKE '%John%';
    >>
    SELECT * FROM customers WHERE name LIKE 'John%';
    ```
    

### 4. NULL 값 처리

- 인덱스 컬럼에서 NULL 값은 기본적으로 인덱스에 저장되지 않음
    
    ```sql
    SELECT * FROM employees WHERE salary IS NULL;
    >>
    ALTER TABLE employees MODIFY salary DEFAULT 0;
    -- 이렇게 테이블 설정을 수정해도 기존의 NULL값으로 입력된 부분은 바뀌지 않음.
    ```
    

### 5. OR 조건이 포함된 경우

- WHERE 절에서 OR 조건이 포함된 경우, 일부 조건에만 인덱스를 사용할 수 있음
    
    ```sql
    SELECT * FROM employees WHERE department_id = 10 OR manager_id = 5;
    >>
    SELECT * FROM employees WHERE department_id = 10
    UNION ALL
    SELECT * FROM employees WHERE manager_id = 5;
    ```
