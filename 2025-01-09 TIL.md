# TIL: 인공지능 원리와 활용
... 이지만 이진 탐색 트리 복습

학습일자: 2025년 01월 09일

## 학습하는데 어떤 문제에 부딪혔는지

며칠 지나긴 했지만, 1월 8일에 본 Recap Test에서 틀린 것을 다시 살펴보고자 했다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

틀린 문제는 이진 탐색 트리와 관련된 부분이라, 해당 부분을 복습했다.

## 어떻게 그 문제를 해결 했는지

복습하는 과정에서 다시 한번 이진 탐색에 대해 공부할 수 있었다.

## 오늘 내가 새롭게 배운 것은 무엇인지

이진 탐색 트리에서 “왼쪽 자식 노드의 값은 부모 노드의 값보다 작고, 오른쪽 자식 노드의 값은 부모 노드의 값보다 크다” 라는 규칙은 탐색하기 편하기 위함임.

이진 탐색 트리의 각 노드가 같은 데이터를 가지는 경우가 없기도 하다.

이를 정리하자면, 다음과 같다.

- 이진 탐색 트리 (Binary Search Tree)
    - 이진 트리 중에서 같은 데이터를 갖는 노드가 없으며, 왼쪽 서브 트리에 있는 모든 데이터가 현재 노드의 데이터보다 작고, 오른쪽 서브 트리에 있는 모든 데이터가 현재 노드의 데이터보다 큰 트리
    - 데이터를 추가하기 위해서는 루트 노드부터 시작해, 루트의 값보다 크면 우측, 작으면 좌측으로 이동해 끝에 추가해주면 된다.
    - 끝 데이터를 삭제하는 것은 그냥 삭제하지만, 삭제할 노드의 자식 노드가 하나인 경우, 그 자식 노드가 그대로 붙지만 삭제할 노드의 자식 노드가 두개인 경우, 위에 접붙이는 느낌으로 한칸씩 올라감
    - 이진 탐색 트리의 균형 문제
        - 이진 탐색 트리가 균형을 이루지 않으면 트리가 편향(skewed) 상태가 될 수 있음
        - 트리가 한 쪽으로 치우쳐져 불균형 상태가 되면, 트리의 높이가 매우 커져 $O(n)$ 시간이 걸리는 비효율적인 연산이 발생할 수 있음.
        - 예를 들어, 입력 데이터가 이미 오름차순이나 내림차순으로 정렬되어 있으면 트리가 한쪽 방향으로만 쭉 뻗어나가는 검색 속도가 느려짐.
        - 이진 탐색 트리가 균형을 이루기 위해서는 트리의 높이를 가능한 한 작게 유지해야 함. 트리의 높이가 $logn$에 가까워야함.
        - 다양한 자기 균형 이진 탐색 트리 알고리즘들을 사용할 수 있음 → AVL 트리, 레드-블랙 트리 등
