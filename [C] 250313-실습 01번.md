# 추가문제 1

```C
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#define MAX_ROWS 5
#define MAX_COLS 5

void print_array(int*, int, int);
void delete_row(int*, int*, int, int)

int main() {

	const int MAX_ROWS = 5;
	const int MAX_COLS = 5;
	int array[MAX_ROWS][MAX_COLS];
	int *ptr_array[MAX_ROWS]; // 배열의 행을 가리킬 포인터 배열
	int rows, cols; // 3, 4

	// 사용자 입력 받기
	printf("행과 열의 수를 입력하세요? ");
	scanf("%d %d", &rows, &cols);

	if (rows > MAX_ROWS || cols > MAX_COLS || rows <= 0 || cols <= 0) {
		printf("오류\n");
		return 1;
	}

	// 배열에 값이 자동으로 들어가기

	for (int i = 0; i < rows; i++) {
		ptr_array[i] = array[i]; // 각 행을 가리키도록 포인터 배열 설정
		for (int j = 0; j < cols; j++) {
			array[i][j] = (i * 10) + j;
		}
	}

	// 출력하기
	printf("배열의 초기값은 자동으로 입력됩니다.\n");
	print_array(ptr_array, rows, cols); // 함수 호출

	// 삭제하기
	int delete_index;
	printf("삭제하려는 행의 인덱스 번호를 입력하세요?");
	scanf("%d", &delete_index);

	printf("수정된 배열 출력\n");
	delete_row(ptr_array, &rows, delete_index, cols);

	print_array(ptr_array, rows, cols);

	return 0;
}

void print_array(int* ptr_array[MAX_ROWS], int rows, int cols) {
	for (int i = 0; i < rows; i++) {
		for (int j = 0; j < cols; j++) {
			printf("%4d", ptr_array[i][j]);
		}
		printf("\n");
	}
}

void delete_row(int* ptr_array[MAX_ROWS], int* rows, int delete_index, int cols) {
	if (delete_index < 0 || delete_index >= rows) {
		printf("오류\n");
		return 1;
	}
	// 행을 한칸씩 위로 이동하는 반복문
	for (int i = delete_index; i < *rows - 1; i++) {
		ptr_array[i] = ptr_array[i + 1];
	}
	(*rows)--;
}

```
