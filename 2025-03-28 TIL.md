# TIL: OCI 관련 에러 해결하기

학습일자: 2025년 03월 28일

## 학습하는데 어떤 문제에 부딪혔는지

OCI를 이용해 DB에 접근하려는데 ORA-24374 에러가 발생했다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

무슨 에러인지 검색에 들어갔다.

## 어떻게 그 문제를 해결 했는지

인터넷 검색을 통해 해결방안을 찾고, 실행했다.

## 오늘 내가 새롭게 배운 것은 무엇인지

```sql
ORA-24374: define not done before fetch or execute and fetch
```

- `FETCH` 또는 `EXECUTE AND FETCH`를 실행하기 전에 **define(정의) 작업이 수행되지 않았을 때** 발생하는 오류.
- 이는 일반적으로 SQL을 실행한 후 결과를 받아올 **출력 변수를 정의하지 않았을 때** 발생함.
- 특히 OCI(Oracle Call Interface)에서 `OCIStmtExecute`를 호출한 후 `OCIDefineByPos`를 적절히 설정하지 않은 경우에 발생할 가능성이 크다.

이를 해결하기 위해서는,

- `SELECT` 문을 실행하기 전에 **반환할 컬럼을 위한 define(정의) 작업**이 수행되었는지 확인한다.
- `OCIDefineByPos()`를 사용하여 SELECT 결과를 저장할 변수를 미리 바인딩한다.
- 코드가 `OCIStmtExecute()`를 호출한 후 바로 `OCIFetch()`를 호출하는 경우, `OCIDefineByPos()`가 생략되지 않았는지 확인한다.
