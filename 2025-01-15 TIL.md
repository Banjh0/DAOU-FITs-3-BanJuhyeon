# TIL: SQL과 SELECT 순서

학습일자: 2025년 01월 15일

## 학습하는데 어떤 문제에 부딪혔는지

실습 과정에서 SQL 쿼리를 작성하는데 어려움을 느꼈다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

강의 내용을 복습하고, 관련된 내용을 찾고자 했다. 

## 어떻게 그 문제를 해결 했는지

다른 답과 비교해보면서 놓친 부분을 찾고, 수정했다.

## 오늘 내가 새롭게 배운 것은 무엇인지

실습과제 6번을 진행하며, 제출하고 난 뒤 부족한 부분을 수정해서 재 제출했다.

```sql
-- 승인된 대출(APPROVED)의 총 대출 금액과 해당 대출 건수를 출력하시오.
SELECT SUM(AMOUNT) AS "총 대출 금액", COUNT(*) AS "대출 건수" FROM LOANS WHERE STATUS = 'APPROVED';

-- 각 월별 총 거래 금액을 계산하고, 거래 금액이 10,000 이상인 달만 출력하시오.
SELECT TO_CHAR(TRANSACTION_DATE, 'MONTH') AS "거래 금액이 10,000 이상인 달",
	SUM(AMOUNT)
FROM TRANSACTIONS 
GROUP BY TO_CHAR(TRANSACTION_DATE, 'MONTH') 
HAVING SUM(AMOUNT) >= 10000;

-- 각 지점에서 승인된 대출(APPROVED)의 평균 대출 금액과 총 대출 금액을 계산하시오.
SELECT BRANCH_ID,
    AVG(AMOUNT) AS "승인된 평균 대출 금액", 
    SUM(AMOUNT) AS "승인된 총 대출 금액" 
FROM LOANS 
WHERE STATUS = 'APPROVED' 
GROUP BY BRANCH_ID;
```

동시에, SELECT 절을 작성하는데 관련하여 작성 순서와 작동 순서를 배울 수 있었다.

### SELECT 문법 작성 순서

SELECT → FROM → WHERE → GROUP BY → HAVING → ORDER BY

### SELECT 실행 작동 순서

FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY
cf) 따라서 SELECT절의 ALIAS는 ORDER BY에서만 가능

FROM을 통해 조회 테이블을 확인하고,

WHERE을 통해 데이터 추출 조건을 확인한 후,

GROUP BY를 통해 컬럼을 그룹화하고

HAVING을 통해 그룹화 조건을 확인한다.

SELECT를 통해 데이터를 추출하고

ORDER BY를 통해 추출된 데이터를 정렬한다.
