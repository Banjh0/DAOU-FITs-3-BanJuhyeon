# TIL: JAVA 메모리 구조

학습일자: 2025년 02월 04일

## 학습하는데 어떤 문제에 부딪혔는지

JVM 메모리 구조에 대해 배웠지만, 실제 코드에 있어 작동 순서를 파악하는 점이 어려웠다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

작성된 코드에서 순서를 확인하고자 했다.

## 어떻게 그 문제를 해결 했는지

강의 중 정리된 내용을 통해서 천천히 과정을 복습했다.

## 오늘 내가 새롭게 배운 것은 무엇인지

JVM의 메모리 구조는 크게 메소드 영역, 스택, 그리고 힙으로 구분된다.

우선적으로 사용하는 클래스에 대한 정보를 메서드 영역으로 올리는데, main 함수가 엔트리포인트이긴 하지만, 수행되는 시점은 나중이다.

메서드가 호출되면 해당 메서드를 위한 공간이 스택에 잡히는데, 이곳에서 영역이 잡히면 관련된 지역 변수들이 올라간다. 

어디서 만들어지던, 인스턴스는 무조건 힙에 만들어진다. 이때 인스턴스의 생성을 통해 클래스의 필드에 대한 공간이 힙에 만들어지는 것.

```java
package mypackage;

// 객체 연습
public class InstanceTest {

    // static field 또는 static variable, class variable
    static int a = staticCall("1번");
    // instance variable
    int b = staticCall("2번");

    // static method, class method(인스턴스 없이 사용 가능해서)
    static public int staticCall(String msg) {
        System.out.println(msg);
        return 100;
    }

    // constructor
    public InstanceTest() {
        this.b = staticCall("3번");
    }

    // entry point
    public static void main(String args[]) {
        System.out.println("4번");
        int c = staticCall("5번");
        InstanceTest d = new InstanceTest();
    }
}
```

따라서 본 코드를 실행해보면 

1번 → 4번 → 5번 → 2번 → 3번 순서로 출력된다.
