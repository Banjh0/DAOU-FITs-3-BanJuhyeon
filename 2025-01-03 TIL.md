#TIL

**다우기술 FITs 3기 반주현**

```
print("Hello World")
```

# TIL: 컴퓨터 구조와 운영체제

학습일자: 2025년 01월 03일

## 학습하는데 어떤 문제에 부딪혔는지

---

컴퓨터 구조와 운영체제에 대한 부분을 처음 배웠다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

---

강의를 열심히 들었다.

## 어떻게 그 문제를 해결 했는지

---

강의를 열심히 들은 후, 중요한 내용을 중심으로 다시 정리해 보았다.

## 오늘 내가 새롭게 배운 것은 무엇인지

---

- 컴퓨터 구조
    - 컴퓨터(하드웨어)의 구성요소(중앙처리장치, 주기억장치, 보조기억장치, 입출력장치)
        - 중앙처리장치(CPU)
            - 명령어를 해독하고 실행.
            - 제어장치, 연산장치, 레지스터 집합으로 구성됨
                - 제어장치: CPU에서 일어나는 모든 작업을 통제하고 관리
                - 연산장치: 제어장치의 신호에 따라 산술 연산과 논리 연산 수행
                - 레지스터 집합: CPU에서 명령어를 수행하는 동안 필요한 정보 저장
        - 주기억장치
            - 실행중인 프로그램과 실행에 필요한 데이터를 저장.
            - RAM, ROM
                - RAM(Random Access Memory): 현재 작업중인 데이터를 저장, 휘발성
                - ROM(Read Only Memory): 영구 메모리, 휘발 X, BIOS등에 적합
        - 보조기억장치
            - 데이터를 장기적으로 저장할 때 사용 + CPU가 현재 필요로 하지 않는 프로그램과 데이터를 저장.
        - 입출력장치
            - 하드웨어 인터럽트(Hardware Interrupt)
                - 하드웨어 장치가 CPU에 알리기 위해 발생시키는 신호.
                - 외부 하드웨어 장치나 내부 장치가 처리해야 할 작업이 생겼을 때 CPU가 현재 수행중인 작업을 일시적으로 중단하고, 하드웨어 장치가 요구하는 작업을 처리한 뒤 이전 작업으로 돌아감.
            - 입출력장치의 성능을 향상시킬 수 있는 방법?
                1. 입출력 채널 분리
                2. DMA (Direct Memory Access): CPU 개입 없이 입출력장치를 처리.
                3. 버퍼: 전송속도 차이를 완화. 느린 입출력 장치에서 데이터를 모아 한번에 옮김.
                4. 프린터기에서는 “스풀”을 사용. 순차적으로 출력. 이를 사용하지 않으면 여러 사람이 공통적으로 사용하는 경우 순서가 엉망.
            - 멀티프로세서와 멀티쓰레드
                - 멀티프로세서는 두 개 이상의 프로세서를 하나의 컴퓨터 시스템에서 동시에 사용하여 작업을 처리하는 아키텍처. 각 프로세서가 각각의 레지스터와 캐시를 가져, 시스템 버스를 통해 메인 메모리를 공유 → 한 번에 여러 작업을 동시에 처리.
                - 멀티쓰레드는 하나의 프로세스 내에서 여러 쓰레드를 실행하는 방식. 여러 쓰레드가 동시에 실행되어 CPU 자원을 효율적으로 활용. 쓰레드간 통신이 프로세스간 통신보다 빠름, 단 공유 자원 문제로 불안정할 가능성 존재.
                - → 두 개 이상의 프로세스가 서로의 자원을 기다리다 멈추는 데드락(Deadlock) 발생 가능
        - cf) 버스
            - CPU와 다른 컴퓨터 부품들 간 데이터를 전송하는 통로
            - 주소버스, 데이터버스, 제어버스로 구성 → 3가지 합쳐서 시스템 버스(FSB)
                - 주소버스(Address Bus): 메모리 + 하드디스크에 존재, 메모리 주소 레지스터(MAR)와 연결. 단방향 버스
                - 데이터버스(Data Bus): 모든 장치 간에 데이터가 이동하는 통로, 메모리 버퍼 레지스터(MBR)와 연결, 양방향 버스.
                - 제어버스(Control Bus): CPU의 제어 장치와 연결, 주변 장치에 이상이 있는 경우 오류신호를 제어 장치에 전달. 양방향 버스.
    
    - 진법(진수변환)
        - 10진수를 2진수, 16진수로 변환하는 것은 0이 나올때 까지 변환할 진수의 숫자로 나누고, 나머지를 기록해 이를 역으로 읽으면 됨.
        - 반대로 2진수, 16진수를 10진수로 변환하기 위해서는 우측부터 각 자리수를 해당 진수의 0제곱으로 시작해 하나씩 늘리고 해당하는 자리수의 숫자를 곱해 모두 다 더하면 10진수로 변환 가능.
        - 2진수 → 16진수는 2진수를 우측부터 4개씩 구분해 4자리의 수를 더한 것이 16진수의 한 자리가 되는 것.
        - 16진수 → 2진수는 반대로 16진수의 한 자리수를 모두 더한 것을 4개로 나누면 됨.

- 운영체제
    - 컴퓨터를 관리하는 소프트웨어, 크게 데스크탑 운영체제, 스마트폰 운영체제, 서버용 운영체제, 임베디드 운영체제로 구분
    - 운영체제의 역할
        - 컴퓨터 자원관리
        - 사용자나 프로그램으로부터 자원 보호
        - 자원에 접근할 수 있는 인터페이스 제공
    - 운영체제는 커널과 인터페이스로 구성
        - 커널: 운영체제의 핵심적인 기능을 모아 놓은 것
            - 커널의 주요 기능
                - 프로세스 관리
                    - 프로세스란, 프로그램이 메모리에 올라가면 CPU에 실행될 명령이 나열되는데 그것들이 프로세스. 이게 Queue에 들어가면, FIFO로 실행될 수 있지만, 이를 관리하는 다양한 알고리즘 존재.
                    - 프로세스의 수행 정보를 기다리다 멈추는 데드락 상황 발생 가능
                - 메모리 관리
                    - 컴퓨터 등을 실행하는데 있어 가상메모리 사용.
                    - 페이징 교체 알고리즘이 존재
                    - cf) 스왑: 프로그램에서 메모리에 올리지 않은 부분을 별도의 공간에 저장, 메모리가 부족해 쫓겨난 프로세스를 모아두는 스왑 영역에 보관. 저장장치에 존재하는 스왑 영역이지만, 메모리 관리자가 관리함.
                    - 가상 메모리 시스템에서의 기술 중 하나가 스왑.
                - 파일 시스템
                    - 파일시스템 메타데이터의 구성요소가 회사마다 달라, 리눅스는 BFS라는 가상파일시스템으로 이를 해결하려 노력중.
                - 입출력 관리
                    - 사용자의 프로그램은 OS를 통해서만 접근 가능. I/O장치에 대한 접근을 system call을 통해 접근.
                    - hardware interrupt 또한 커널이 관리,
                - 프로그램 간 통신 환경 제공
                    - 프로세스는 독립적인 실행객체 → 타 프로세스의 영향을 받지 않음
                    - 즉, 프로세스 간 통신이 어려운 상황
                    - 이를 해결하기 위한 것이 IPC(Inter-Process Communication).
                    - 이 때, 프로세스가 Read, Write 모두가 필요할 때 프로세스 간 공유 메모리가 데이터 자체를 공유하는 것을 커널이 관리함. 이는 PIPE 등과 다르게 곧바로 메모리에 접근할 수 있으므로 IPC 중 가장 빠르게 작동함
                    
                    ---
                    
                    - 프로세스는 독립적 메모리 공간 할당 → 프로세스간 통신 불가
                    - IPC 기능 (커널)을 통해 활용
                    - Shared Memory & Message Parsing
                    - 커널 공유메모리 할당을 통해 메모리 받고, 커널의 관여 없이 수행 → 별도의 동기화 작업 필요
                    - 커널을 공유해 메세지 전달, 별도의 동기화가 필요 없지만 경유로 인해 속도가 상대적으로 느림 ex. PIPE, Socket 통신
        - 인터페이스: 커널에 명령을 내리고 결과를 사용자, 응용 프로그램에 전달.
    - 디렉터리(Directory)
        - 운영체제에서 디렉터리란, 서로 관련 있는 파일을 모아 놓은 것 (= 폴더)
        - hierarchy한 계층 구조를 지니고 있음.
