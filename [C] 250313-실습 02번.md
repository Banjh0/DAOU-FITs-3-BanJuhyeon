# 추가문제 2

```C
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#define ROWS 3
#define COLS 3

int sum(int ** ptr, int rows, int cols) {
	int total = 0;
	for (int i = 0; i < rows; i++) {
		for (int j = 0; j < cols; j++) {
			total += ptr[i][j];
		}
	}
	return total;
};
int max(int** ptr, int rows, int cols) {
	int max_value = ptr[0][0];
	for (int i = 0; i < rows; i++) {
		for (int j = 0; j < cols; j++) {
			if (max_value < ptr[i][j]) {
				max_value = ptr[i][j];
			}
		}
	}
	return max_value;
};
int min(int** ptr, int rows, int cols) {
	int min_value = ptr[0][0];
	for (int i = 0; i < rows; i++) {
		for (int j = 0; j < cols; j++) {
			if (min_value > ptr[i][j]) {
				min_value = ptr[i][j];
			}
		}
	}
	return min_value;
};
void square(int** ptr, int rows, int cols) {
	for (int i = 0; i < rows; i++) {
		for (int j = 0; j < cols; j++) {
			ptr[i][j] *= ptr[i][j]; // 제곱
		}
	}
};
void print_array(int** ptr, int rows, int cols) {
	for (int i = 0; i < rows; i++) {
		for (int j = 0; j < cols; j++) {
			printf("%3d", ptr[i][j]);
		}
		printf("\n");
	}
}

int main() {

	int array[ROWS][COLS] = { 
								{0, 1, 2},
								{3, 4 ,5},
								{6, 7, 8}
							};
	// 이중 포인터 배열 설정
	int* ptr_array[ROWS];
	for (int i = 0; i < ROWS; i++) {
		ptr_array[i] = array[i];
	}

	// 함수 포인터 배열 (반환값 void인 함수들)
	void* operations[4] = {sum, max, min, square};
	int choice;
	while (1) {
		printf("0: 합, 1: 최대값, 2:최소값, 3:제곱 -1:종료 : ");
		scanf("%d", &choice);
		if (choice == -1) {
			break;
		}
		if (choice < 0 || choice > 3) {
			printf("번호 선택 오류\n");
			continue;
		}
		if (choice == 3) { // square인 경우 반환형은 void형
			((void (*)(int**, int, int)) operations[choice]) (ptr_array, ROWS, COLS);
			printf("배열의 제곱값은\n");
			print_array(ptr_array, ROWS, COLS);
		}
		else { // 반환형은 int형
			int result = ((int (*)(int**, int, int)) operations[choice]) (ptr_array, ROWS, COLS);
			printf("결과값은 %d\n", result);
		}
	}

	return 0;
}

```
