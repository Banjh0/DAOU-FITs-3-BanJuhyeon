# 실습과제 10-1

```C
#include <stdio.h>

int main() {

	int sel, radius;
	printf("========================\n");
	printf(" == 1. 원의 둘레 구하기       ==\n");
	printf(" == 2. 원의 넓이 구하기       ==\n");
	printf(" == 3. 구의 부피 구하기       ==\n");
	printf(" == 4. 그만두기               ==\n");
	printf("========================\n");
	while (1) {
		printf("\n원하는 내용은? ");
		scanf_s("%d", &sel);
		if (sel == 4) {
			break;
		}
		printf("\n>> 반지름은? ");
		scanf_s("%d", &radius);
		switch (sel) {
		case 1: printf("\n>> 반지름이 %d인 원의 둘레는 %.2lf\n", radius, 2 * 3.141592 * radius);
			continue;
		case 2: printf("\n>> 반지름이 %d인 원의 넓이는 %.2lf\n", radius, radius * radius * 3.141592);
			continue;
		case 3: printf("\n>> 반지름이 %d인 구의 부피는 %.2lf\n", radius, (4 * 3.141592 * radius * radius * radius) / 3.0);
			continue;
		}
	}

	return 0;
}
```

# 실습과제 10-2

```C
#include <stdio.h>

int main() {

	int n;
	printf("정수 n을 입력 : ");
	scanf_s("%d", &n);
	int total = 0;
	for (int i = 1; i <= n; i++) {
		if (i % 2 == 0) {
			total += i;
		}
	}
	printf("정수 1에서 %d 이하 짝수들의 합은 %d입니다.", n, total);

	return 0;
}
```
