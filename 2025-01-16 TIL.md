# TIL: SQL 문제풀이

학습일자: 2025년 01월 16일

## 학습하는데 어떤 문제에 부딪혔는지

SQL 문제풀이를 하는데 뒤로 갈수록 풀이에 어려움을 느꼈다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

기존의 강의 내용을 복습하고, 여러 자료를 찾고자 했다.

## 어떻게 그 문제를 해결 했는지

문제 풀이를 여러 번 하다 보다 일부는 풀 수 있었고, 풀면서도 배우는 점이 많았다.

## 오늘 내가 새롭게 배운 것은 무엇인지

월 별 신규 고객 수를 최근순으로 조회하는 문제가 있었다.

```sql

SELECT TO_CHAR(CREATED_AT, 'YYYY-MM') "MONTHS",
    COUNT(*) "고객 수"
FROM CUSTOMERS
GROUP BY CREATED_AT
ORDER BY CREATED_AT DESC;

```

위 문제에서, 위에 같이 하면 오류가 발생함을 발표하면서 알 수 있었다. 이는 CREATED_AT으로 GROUP BY를 하면 년 월 일 중 일까지 포함되서 같은 월에 CREATED_AT한 자료들도 그 안에서 같은 그룹으로 묶이지 않고 나뉨을 확인했다.

동시에, GROUP BY를 TO_CHAR(CREATED_AT, 'YYYY-MM')로 수정한 후 ORDER BY에서 에러가 발생했다. 이 또한 위의 ALIAS를 이용한 MONTHS를 넣어야지 정상적으로 출력이 가능했다.

이러한 내용을 수정한 코드는 아래와 같다.

```sql
SELECT TO_CHAR(CREATED_AT, 'YYYY-MM') "MONTHS",
    COUNT(*) "고객 수"
FROM CUSTOMERS
GROUP BY TO_CHAR(CREATED_AT, 'YYYY-MM')
ORDER BY MONTHS DESC;
```

또한, 문제를 풀면서 특히 서브쿼리를 어느정도 요하는(ROWNUM을  사용한다던가, 기타 문제들) 문제에 대해서는 풀이가 부족하다는 사실을 알 수 있었다. 따라서 우선적으로 그 중 한 문제를 풀어 감을 익히고자 했다.

대출 금액이 가장 높은 상위 5명의 고객의 이름과 대출 금액을 조회하세요. (단, 대출 금액이 동일한 경우 고객의 이름으로 오름차순) 라는 문제를 우선 풀어보고자 했다.

```sql
SELECT "고객",
    "대출 금액"
FROM (
    SELECT C.NAME "고객",
        SUM(L.AMOUNT) "대출 금액"
    FROM CUSTOMERS C
        JOIN LOANS L
            ON C.CUSTOMER_ID = L.CUSTOMER_ID
    GROUP BY C.NAME,
        L.AMOUNT
    ORDER BY L.AMOUNT DESC,
        C.NAME
)
WHERE ROWNUM <= 5;
```

이를 기반으로 하는 동시에 동기들의 코드 또한 참고해 더 많은 문제를 풀어보고자 한다.
