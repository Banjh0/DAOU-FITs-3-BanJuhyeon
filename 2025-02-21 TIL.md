# TIL: MyBatis

학습일자: 2025년 02월 21일

## 학습하는데 어떤 문제에 부딪혔는지

mapper에 `CONCAT('%', ?, '%')`형식으로 파라미터를 지정했는데, 관련해서 에러가 발생했다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

일단 `${}`를 통해 구현하면 작동은 하는 것으로 보아, 들어오는 데이터값이 잘못되지 않았다는 것을 확인할 수 있었다.

## 어떻게 그 문제를 해결 했는지

검색 결과, CONCAT함수를 활용하는데 잘못이 있었다는 것을 확인할 수 있었다.

## 오늘 내가 새롭게 배운 것은 무엇인지

Oracle SQL에서는 `CONCAT` 함수를 사용할 수 있지만, `CONCAT` 함수는 두 개의 인자만 받을 수 있다는 사실을 알았다. 

세 개 이상의 문자열을 연결하려면 `||` 연산자를 사용하거나, 여러 번 `CONCAT` 함수를 중첩해서 사용해야한다.

따라서 `WHERE문` 에서 `WHERE TITLE LIKE CONCAT('%', #{keyword}, '%') OR CONTENT LIKE CONCAT('%', #{keyword}, '%')` 는 `CONCAT` 함수가 2개의 인자만 받을 수 있다는 점에서 잘못된 것이기 때문에

→ `WHERE TITLE LIKE '%' || #{keyword} || '%' OR CONTENT LIKE '%' || #{keyword} || '%’` 와 같이 사용해야한다.
