# TIL: SQL 복습

학습일자: 2025년 02월 26일

## 학습하는데 어떤 문제에 부딪혔는지

MyBatis를 활용하면서 이전에 배운 SQL의 복습이 필요했다. 예를 들면 JOIN이라던가 COMMIT같은 부분에 대해서 복습의 필요성을 느꼈다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

처음에는 기억을 더듬으며 SQL문을 작성했었다. 

## 어떻게 그 문제를 해결 했는지

이전에 정리한 내용을 다시 한 번 복습했다.

## 오늘 내가 새롭게 배운 것은 무엇인지

### INNER JOIN

- 두 테이블 간에 일치하는 데이터만 반환
- 기본적인 JOIN 방식
- 예시
    
    ```sql
    SELECT E.NAME, D.DEPARTMENT_NAME
    FROM EMPLOYEES E
    INNER JOIN DEPARTMENTS D
        ON E.DEPARTMENT_ID = D.DEPARTMENT_ID
    =
    SELECT E.NAME, D.DEPARTMENT_NAME
    FROM EMPLOYEES E, DEPARTMENTS D
    WHERE E.DEPARTMENT_ID = D.DEPARTMENT_ID
    
    >> 위와 아래는 동일.
    ```
    

### LEFT OUTER JOIN

- LEFT 테이블의 모든 행과 일치하는 RIGHT 테이블의 데이터를 반환
- 일치하지 않는 경우 NULL 반환
- 예시
    
    ```sql
    SELECT E.NAME, D.DEPARTMENT_NAME
    FROM EMPLOYEES E
        LEFT OUTER JOIN DEPARTMENTS D
            ON E.DEPARTMENT_ID = D.DEPARTMENT_ID
    >> 사원이 기준
    ```
    

### JOIN 사용 시 유의사항

- JOIN 조건 누락
    - JOIN 조건이 없으면 CROSS JOIN과 동일한 결과 발생
- NULL 처리
    - OUTER JOIN 시 NULL 값을 처리하는 로직 필요
    - 운이 없으면 추후 들어온 데이터에 NULL 이 없으면 없던 에러 발생도 가능.
- 성능
    - 큰 테이블 간 JOIN은 성능 저하 가능, 적절한 인덱스 설정 필요

### COMMIT을 해야 적용된 데이터를 볼 수 있습니다! 분명 DB상에서 SQL문을 돌리면 2개 row가 보이는데, 페이지 상에서는 1개만 보일 때, 이전에 입력한 더미데이터를 COMMIT했는지 확인해야합니다!
