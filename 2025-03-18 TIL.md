# TIL: malloc(), calloc(), realloc()

학습일자: 2025년 03월 18일

## 학습하는데 어떤 문제에 부딪혔는지

동적 메모리 할당에 대해 처음 배우게 되었다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

강의를 열심히 들었다.

## 어떻게 그 문제를 해결 했는지

강의를 열심히 듣고 정리했다.

## 오늘 내가 새롭게 배운 것은 무엇인지

### malloc()

1. 형식
`void* malloc( size_t size );`
`size_t size`는 바이트 크기
`void*` 메모리할당이 성공하면 반환값으로 할당된 메모리 블록의 시작 주소
실패하면 `NULL` 반환
    
    주의 : 리턴타입이 void이므로 해당형으로 형변환해야 한다.
    **`int ptr = (int*)malloc(sizeof(int));`
    
2. 기본적인 사용법
    
    (1) 정수 4바이트를 동적 할당 하여서 값을 넣고 출력하기
    
    ```c
    #include <stdio.h>
    #include <stdlib.h> // malloc(), free()가 정의된 헤더 파일
    
    int main() {
    	int* ptr = (int*)malloc(sizeof(int)); // int 크기만큼 동적 할당
    	
    	if (ptr == NULL) {
    		printf("메모리 할당 실패!\n");
    		return 1;
    	}
    	
    	*ptr = 42; // 할당된 Heap 메모리에 값 저장
    	printf("동적 메모리의 값: %d\n", *ptr);
    	
    	free(ptr); // 메모리 해제
    	
    	return 0;
    }
    ```
    
    - `*ptr = 42` 값을 지정한 후 `*ptr += 100;` 이나 `if(*ptr >= 100) printf("합격")` 같이 자유롭게 사용
    - 여기서 `free(ptr)` 하지 않으면 계속 메모리에 상주 → 메모리 누수
    
    (2) 배열의 크기를 할당하기
    
    ```c
    #include <stdio.h>
    #include <stdlib.h>
    
    int main() {
     int n = 5;
     int* arr = (int*)malloc(n * sizeof(int)); // 정수 5개 크기의 배열 할당
     if (arr == NULL) {
     printf("메모리 할당 실패!\n");
     return 1;
     }
     
     // 동적 배열 초기화
     for (int i = 0; i < n; i++) {
     arr[i] = i + 1;
     }
     
     // 배열 출력
     printf("동적 배열 요소: ");
     for (int i = 0; i < n; i++) {
     printf("%d ", arr[i]);
     }
     printf("\n");
     free(arr); // 메모리 해제
     
     return 0;
    }
    ```
    
3. malloc의 문제점
    - 초기화 되지 않은 메모리 블록은 쓰레기 값이 들어 간다,
    - 초기화가 필요하면 `calloc()` 으로 사용하면 초기값이 자동으로 채워진다.
4.  동적 메모리를 사용한 후 반드시 free() 호출해 줄 것

### calloc()

calloc() (Contiguous Allocation) 함수는 C 언어에서 동적 메모리를 할당하는 함수, 

0으로 초기화된 메모리 블록을 반환.

연속된 여러 개의 요소를 한 번에 할당할 때 유용함.

1. 형식
`void* callock( size_t num, size_t size );`
`num` – 할당할 요소의 개수
`size` – 각 요소의 크기(바이트 단위)
반환값은 성공하면 할당된 메모리 블록의 시작주소, 실패하면 `NULL`
2.  malloc()과 calloc() 비교 
    
    ```c
    #include <stdio.h>
    #include <stdlib.h>
    
    int main() {
     int n = 5;
     
     int* arr1 = (int*)malloc(n * sizeof(int)); // 초기화되지 않음
     int* arr2 = (int*)calloc(n, sizeof(int)); // 0으로 초기화됨
     
     printf("malloc() 사용: ");
     for (int i = 0; i < n; i++) {
     printf("%d ", arr1[i]); // 쓰레기 값이 나올 가능성 있음
     }
     printf("\n");
     
     printf("calloc() 사용: ");
     for (int i = 0; i < n; i++) {
     printf("%d ", arr2[i]); // 항상 0으로 초기화됨
     }
     printf("\n");
     
     free(arr1);
     free(arr2);
     
     return 0;
    }
    
    ```
    
3. `calloc()` 사용시 주의점
    - `calloc()`은 성능이 다소 `malloc()` 보다 느릴 수 있다.
    왜? 할당 후 모든 바이트를 0으로 초기화하기 때문
    - 그래서 0으로 초기화 할 필요 없는 경우 `malloc()`을 사용.
4.  2차원 배열을 할당하는 예제
    
    ```c
    #include <stdio.h>
    #include <stdlib.h>
    
    int main() {
     int rows = 3, cols = 4;
     
     // 2차원 배열 동적 할당
     int** arr = (int**)calloc(rows, sizeof(int*));
     for (int i = 0; i < rows; i++) {
    	 arr[i] = (int*)calloc(cols, sizeof(int));
     }
    
     // 값 입력 및 출력
     for (int i = 0; i < rows; i++) {
    	 for (int j = 0; j < cols; j++) {
    		 arr[i][j] = i * cols + j;
    		 printf("%d ", arr[i][j]);
    		}
    	 printf("\n");
     }
    
     // 메모리 해제
     for (int i = 0; i < rows; i++) {
    	 free(arr[i]);
     }
     free(arr);
    
     return 0;
    }
    ```
    

### realloc()

- `realloc()` 함수는 이미 할당된 메모리 블록의 크기를 변경하는 함수.
- 기존에 할당된 메모리 블록을 새로운 크기로 확장하거나 축소 가넝.
- 크기를 확장할 때 기존 데이터를 그대로 유지하며 새로운 메모리 블록을 할당.
- 기존 메모리 크기를 변경하면서 새로운 메모리 주소를 반환할 수 있기 때문에, 변경된 포인
터를 반드시 저장해야 함.

> The reallocation is done by either:
> 

> a) expanding or contracting the existing area pointed to by ptr, if possible. The contents of the area remain unchanged up to the lesser of the new and old sizes. If the area is expanded, the contents of the new part of the array are undefined.
b) allocating a new memory block of size new_size bytes, copying memory area with size equal the lesser of the new and the old sizes, and freeing the old block.
> 
1. 함수 구조
`void* realloc( void* ptr, size_t size );`
`ptr` – 기존에 할당된 메모리 블록의 포인터
`size` – 새로운 메모리 블록의 크기( 바이트 단위)
반환값 – 성공하면 새로운 메모리 블록의 포인터, 실패하면 `NULL`
2. `malloc()`으로 정수형 배열 5개를 할당한 후 추가로 `realloc()`으로 10으로 늘리는 예제
    
    ```c
    #include <stdio.h>
    #include <stdlib.h>
    
    int main() {
     int n = 5;
     int* arr = (int*)malloc(n * sizeof(int)); // 초기 메모리 할당
     
     if (arr == NULL) {
    	 printf("메모리 할당 실패!\n");
    	 return 1;
     }
     
     // 초기값 설정
     for (int i = 0; i < n; i++) {
    	 arr[i] = i + 1;
     }
     
     printf("초기 배열: ");
     for (int i = 0; i < n; i++) {
    	 printf("%d ", arr[i]);
     }
     printf("\n");
     
     // 메모리 크기 변경 (5 → 10)
     n = 10;
     int* new_arr = (int*)realloc(arr, n * sizeof(int));
     
     if (new_arr == NULL) {
    	 printf("메모리 재할당 실패!\n");
    	 free(arr); // 기존 메모리 해제
    	 return 1;
     }
     
     arr = new_arr; // 새로운 메모리 블록으로 갱신
     
     // 추가된 공간 초기화
     for (int i = 5; i < n; i++) {
    	 arr[i] = (i + 1) * 10;
     }
     
     printf("크기 변경 후 배열: ");
     for (int i = 0; i < n; i++) {
    	 printf("%d ", arr[i]);
     }
     
     printf("\n");
     
     free(arr); // 메모리 해제
     
     return 0;
    }
    ```
    
    - 이 때, `arr = new_arr` 하면서 기존 `arr`이 가지고 있던 Heap 메모리의 값은 운영체제가 FAT, NTFS 등이 빈 공간으로 인식. 약간 빠른 포맷 개념인듯? 그래서 기존 `arr`는 `free()` 안해도 자동으로 됨.
3. 사용시 주의점
    - 메모리 할당에 실패할 때 NULL을 반환,
    - 이때 기존 포인터가 NULL로 변경되면 기존 포인터를 잃게 될 수 있다
    - ⇒ 이에, `realloc()` 을 사용하기 전에 어딘가에 담아둬야함.
