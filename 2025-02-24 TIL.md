# TIL: Git 시작

학습일자: 2025년 02월 24일

## 학습하는데 어떤 문제에 부딪혔는지

프로젝트를 진행하면서 Git 사용의 필요성을 느껴, 진행사항을 정리해보았다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

인터넷에 작성된 글들을 확인했다.

## 어떻게 그 문제를 해결 했는지

Git Bash에 입력해서 테스트해보았다.

## 오늘 내가 새롭게 배운 것은 무엇인지

1. 우선 Git의 브랜치가 master로 되어있는 반면, GitHub에는 main으로 되어있다. 이는,
`git config --global init.defaultBranch main` 를 통해 변경할 수 있다.
2. 로컬상의 Git과 remote를 연결하기 위해서는
`git remote add origin 레포링크` 를 통해 해당 GitHub을 remote로 등록한다.
3. 이후 commit을 위해 본인의 정보를 등록하는데,
    
    `git config --global user.email "이메일주소"`
    `git config --global user.name "이름"`
    
    이렇게 등록하면 git commit이 가능해진다.
