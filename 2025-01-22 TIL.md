# SQL: 계층쿼리와 그룹함수

학습일자: 2025년 01월 22일

## 학습하는데 어떤 문제에 부딪혔는지

계층 쿼리와 그룹 함수에 대한 문제를 풀었다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

강의를 열심히 듣고, 정리했다.

## 어떻게 그 문제를 해결 했는지

강의를 기반으로 열심히 문제를 풀어 개념을 익힐 수 있게 했다.

## 오늘 내가 새롭게 배운 것은 무엇인지

계층 쿼리에서는 가장 깊은 depth를 구하는 부분이 어려웠다.

```sql
-- 쇼핑 카테고리 데이터의 순방향 계층 구조에서 depth가 가장 깊은 LEVEL 값을 구하시오.
SELECT MAX(LEVEL)
FROM SHOPPING_CATEGORIES
START WITH PARENT_CATEGORY_ID IS NULL
CONNECT BY PRIOR CATEGORY_ID = PARENT_CATEGORY_ID
;
```

의외로, LEVEL의 MAX값을 통해 가장 깊은 depth를 구할 수 있었다. LEVEL에 집계함수를 활용할 수 있다는 사실을 알 수 있었다.

그룹 함수의 특징은 다음과 같다.

- 데이터를 특정 기준으로 그룹핑하여 요약, 집계하는 데 사용
- 데이터 분석 및 보고서를 생성할 떄 유용한 기술
- 다차원 데이터를 요약하는 데 활용 → 데이터를 그룹핑을 해서 합계 + 소계 + 총계.. 등과 같이 자유자재로 컨트롤하는 함수.

주요 그룹 함수는 아래와 같이 존재한다.

| 그룹함수 | 설명 |
| --- | --- |
| ROLLUP | 특정 컬럼 순서대로 부분 합계 및 총합계를 계산하는 함수 |
| CUBE | 모든 컬럼의 가능한 조합에 대해 다차원 집계를 계산하는 함수 |
| GROUPING SETS | 지정된 그룹핑 조합에 대해서만 집계를 계산하는 함수 |
| GROUPING | 집계 데이터와 실제 데이터를 구분하는 데 사용하는 함수 |

큐브는 롤업의 확장으로 그룹핑 할 수 있는 모든 데이터를 그룹핑하며,

그룹핑 셋츠는 각각의 인수들로 그룹핑 한 것을 UNION하는 개념이라 이해하기는 쉬워보였다.

다만, 복잡한 GROUPING SETS를 보면 아래와 같은데,

```sql
GROUPING SETS(A, B)
A로 그룹핑, B로 그룹핑

GROUPING SETS(A, B, ())
A로 그룹핑, B로 그룹핑, 총합계

GROUPING SETS (A, ROLLUP(B))
A로 그룹핑, B로 그룹핑, 총합계

GROUPING SETS (A, ROLLUP(B, C))
A로 그룹핑, B, C로 그룹핑, B로 그룹핑, 총합계

GROUPING SETS (A, B, ROLLUP(C))
A로 그룹핑, B로 그룹핑, C로 그룹핑, 총합계
```

이를 이용한 문제를 풀다 보면 사용 아직도 어려움을 느껴 조금 더 공부해야겠다는 생각이 든다.
