# TIL: C언어의 자료형

학습일자: 2025년 03월 05일

## 학습하는데 어떤 문제에 부딪혔는지

자료형을 공부하는데 기존에 배운 Java와 차이를 보이는 부분을 발견했다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

책을 보면서 공부했다.

## 어떻게 그 문제를 해결 했는지

강의를 듣고, 책을 다시 봤다.

## 오늘 내가 새롭게 배운 것은 무엇인지

`int a` 는 (signed) int와 같은 느낌이다. Java와 같은 느낌으로 음수와 양수 모두가 가능하다,

새로운 형식으로 `unsigned int` 라는 자료형도 있는데, 크기는 4byte로 같지만, 음수가 아닌 모두 양수만 가능하기 때문에 범위가 0부터 약 42억까지 가능하다.

특이사항으로 `int` 자료형은 언더플로우도 가능한데, `int a = -2147483647` 이라고 할 때, 여기에 -2를 하면 오히려 최대값이라고 할 수 있는 +2147483647이 된다.

`short` 도 마찬가지인데, 16비트까지 표현할 수 있지만 `unsigned short` 자료형은 약 65000 까지 표현할 수 있다.
