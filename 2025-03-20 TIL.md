# TIL: 파일 데이터 수정(덮어쓰기)

학습일자: 2025년 03월 20일

## 학습하는데 어떤 문제에 부딪혔는지

파일의 내용을 수정하는데, 중간 부분에 위치한 항목을 수정하는데 어려움이 있었다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

`fprintf()`를 이용했지만, 오류가 발생했다.

## 어떻게 그 문제를 해결 했는지

`fseek()` 과 `ftell()` 을 이용해 중간에 위치한 항목이라도 이를 수정할 수 있었다. 이를 위해 인터넷 검색 등을 통해 정보를 수집하고, 이용하는 방법을 찾아보았다.

## 오늘 내가 새롭게 배운 것은 무엇인지

파일 포인터와 `ftell()`, `fseek()` 개념

- `ftell(FILE *file)`
    - 현재 파일 포인터의 위치(바이트 단위)를 반환.
    - 즉, 파일에서 데이터를 읽기 직전의 위치를 저장하는 데 사용.
- `fseek(FILE *file, long offset, int origin)`
    - 파일 포인터를 원하는 위치로 이동.
    - `origin`의 값에 따라 이동 방식이 다름:
        - `SEEK_SET`: 파일의 처음(0 바이트)에서 `offset`만큼 이동
        - `SEEK_CUR`: 현재 위치에서 `offset`만큼 이동
        - `SEEK_END`: 파일 끝에서 `offset`만큼 이동

---

### **1. `ftell(file)`로 위치 저장**

```c
pos = ftell(file);
```

- 현재 파일 포인터의 위치(해당 계좌의 데이터가 시작하는 지점)를 저장.

### **2. `fscanf()`로 계좌 정보 읽기**

```c
fscanf(file, "%d %s %lf", &a.id, a.name, &a.balance);
```

- 파일에서 한 줄을 읽어와서 ID, 이름, 잔액을 변수에 저장.

### **3. 원하는 ID를 찾으면 `fseek()`으로 되돌아감**

```c
fseek(file, pos, SEEK_SET);
```

- `fseek()`을 사용하여 수정할 계좌의 시작 위치로 파일 포인터를 이동.
- 그 후 `fprintf()`로 데이터를 덮어쓰기.

### 예시)

### **1. `ftell()`로 위치 저장**

처음 파일을 읽을 때, `ftell()`이 저장하는 위치:

```
|  위치 (바이트)  |  내용  |
|--------------|--------------|
|  0          |  12345       |
|  6          |  Hong        |
|  11         |  10000.00    |
|  20 (줄 끝)  |  (개행)      |
```

- **첫 번째 줄을 읽기 직전, `pos = 0` 저장.**
- 이후 `fscanf()`가 실행되면서 파일 포인터가 개행(`\n`) 다음 위치(20)로 이동.

### **2. ID가 `12345`라면 `fseek()`으로 되돌아가기**

```c
fseek(file, pos, SEEK_SET);
```

- 파일 포인터를 `pos = 0` (해당 계좌가 시작하는 위치)로 이동.

### **3. `fprintf()`로 새로운 값 덮어쓰기**

```c
fprintf(file, "%d %s %.2lf\n", a.id, a.name, a.balance);
```

- **기존 위치에 새 데이터를 덮어씀** → 잔액이 변경된 새로운 데이터가 저장됨.

  

  ⇒ 기존 내용이 그대로 유지되면서 `12345` 계좌의 잔액만 변경됨.
