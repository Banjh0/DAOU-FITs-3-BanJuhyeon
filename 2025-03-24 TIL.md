# TIL: C언어 모듈화

학습일자: 2025년 03월 24일

## 학습하는데 어떤 문제에 부딪혔는지

C언어에서 모듈화를 처음 배우게 되었다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

강의를 열심히 들었다.

## 어떻게 그 문제를 해결 했는지

강의를 듣고, 정리를 했다.

## 오늘 내가 새롭게 배운 것은 무엇인지

### 모듈화

모듈화는 프로그램을 독립적인 기능을 수행하는 작은 단위 (= 모듈)로 나누어 개발하는 소프트웨어 설계 기법이다.

- 모듈은 각각의 기능을 담당하며 서로 독립적으로 작성, 수정, 테스트할 수 있어 유지보수성과 재사용성이 증가한다.
- 논리적으로 같은 등급의 소스 파일을 계속해서 분할하는 것
- 소스파일, 혹은 어떤 함수에서 일어나는 일의 책임을 그 소스 파일과 그 함수가 지도록 하는 것이다.

### C언어에서의 모듈화

C언어에서는 헤더파일과 소스파일을 분리하여 모듈화를 구현한다.

- 헤더파일(.h) : 함수 선언 및 전역 변수를 정의하는 부분
- 함수 선언, 매크로, typedef
- 소스파일(.c) : 함수를 실제로 구현하는 부분
- 함수 정의 및 내부 로직 구현
- 위와 같이 함수선언(인터페이스)와 구현을 나누어 관리한다.

### 모듈화시 고려할 점

1. 하나의 모듈은 한 가지 역할만 수행해야 한다.
2. 인터페이스와 구현 분리, 헤더파일과 소스파일을 분리한다.
3. 높은 응집도와 낮은 결합도, 관련 기능끼리 묶고 모듈 간 의존성을 최소화한다.
4. 재사용성을 고려해 공통 기능은 별도의 모듈로 분리한다.
5. 변경 가능성을 고려해 확장 가능하도록 설계하여 유지보수를 쉽게 한다.
