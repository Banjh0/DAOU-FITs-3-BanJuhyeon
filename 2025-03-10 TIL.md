# TIL: 매크로 함수

학습일자: 2025년 03월 10일

## 학습하는데 어떤 문제에 부딪혔는지

매크로 함수를 처음 접하게 되었다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

강의를 열심히 들었다.

## 어떻게 그 문제를 해결 했는지

열심히 들은 강의를 정리했다.

## 오늘 내가 새롭게 배운 것은 무엇인지

매크로 함수란, 일반함수 처럼 인수를 갖는 함수이다. 

`#define 매크로함수명(인수…) (치환할 내용의 함수 내용)` 의 형식으로 작성한다.

매크로 함수의 장점으로는 컴파일 속도가 빨라지는데, 이는 컴파일을 할 때 함수가 호출되는 개념이 아니라 호출된 곳에 코드가 직접 삽입되기 때문이다.

전처리기는 매크로 함수가 사용된 곳을 찾아 ()안에 있는 매크로 함수의 인수를 치환할 내용에 매핑하여 문자열 치환을 수행ㅎㄴ다.

단, 이때 주의해야 하는 점으로 치환할 내용, 즉 함수 내용을 반드시 ()를 쳐야한다는 점이다. 

```c
#define power1(x) ( x * x )
#define power2(x) ( (x) * (x) )

int main() {

	int num;
	
	printf("정수를 입력하세요 : ");
	scanf_s("%d", &num);
	printf("%d의 제곱 : % d\n", num, power1(num));
	printf("%d의 제곱 : % d\n", num, power2(num));
	printf("(%d + 2)의 제곱 : % d\n", num, power1(num + 2));
	printf("%d의 제곱 : % d\n", num, power2(num + 2));

	return 0;
}
```

위 코드에서, 연산 순서에 따른 결과값의 차이를 확인할 수 있다.

또한 제한적인 만큼, 크기가 작은 함수만 매크로 함수로 정의하는 것이 좋다고 한다.
