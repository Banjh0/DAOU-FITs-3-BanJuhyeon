# TIL: 다중 포인터 변수

학습일자: 2025년 03월 11일

## 학습하는데 어떤 문제에 부딪혔는지

C를 학습하면서 포인트 변수라는 개념을 처음 맞닥뜨리게 되었다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

강의를 열심히 들었다.

## 어떻게 그 문제를 해결 했는지

강의를 열심히 듣고 복습을 진행했다.

## 오늘 내가 새롭게 배운 것은 무엇인지

```c
#include <stdio.h>

int main() {

	int mm, * pmm; // int mm, int *pmm;, cf) int* mm, pmm == int *mm, int pmm
	mm = 500;
	pmm = &mm;

	// 500 출력
	printf("%d %d\n", mm, *pmm);

	// 다중 포인터 변수
	int** ppmm;
	ppmm = &pmm;
	printf("%p %p\n", &pmm, ppmm);
	printf("%d %d %d\n", mm, *pmm, **ppmm);

	int*** pppmm;
	pppmm = &ppmm;
	printf("%d %d %d %d\n", mm, *pmm, **ppmm, ***pppmm);

	return 0;
}

```

`int mm, * pmm;` 같이 선언문에서의 *(→ 포인터변수)과, `printf("%d %d %d\n", mm, *pmm, **ppmm);` 혹은 `int c = *pa` 같은 실행문에서의 *(→ 간접참조연산자:  ~의 값 / ~주소의 실제 값 )은 다른 의미를 지닌다는 것을 꼭 기억하자!
