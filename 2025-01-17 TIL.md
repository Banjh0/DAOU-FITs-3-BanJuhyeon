# TIL: SQL 문제풀이

학습일자: 2025년 01월 17일

## 학습하는데 어떤 문제에 부딪혔는지

SQL 관련 문제를 해결하면서 일부 문제에서 어려움을 느꼈다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

이전 강의 내용을 다시 확인하기 위해 찾아봤다.

## 어떻게 그 문제를 해결 했는지

잘모르는 부분을 강사님께서 알려주셨다.

## 오늘 내가 새롭게 배운 것은 무엇인지

각 계좌별 거래 회수를 찾기 위해 각 계좌의 거래 횟수를 조회하고 거래 횟수로 오름차순하여 출력하는 문제가 있었다. 단, 거래 횟수가 없는 계좌도 전제한다는 조건이 붙어있었다.

```sql
SELECT A.ACCOUNT_ID "계좌",
COUNT(*) "거래 횟수"
FROM ACCOUNTS A
LEFT OUTER JOIN TRANSACTIONS T
ON A.ACCOUNT_ID = T.ACCOUNT_ID
GROUP BY A.ACCOUNT_ID
ORDER BY COUNT(*);
```

처음에는 위와 같이 작성해서 결과가 나온 것 처럼 보였지만, 문제가 있었다.

거래 횟수가 NULL이어도 COUNT(*)를 하게 되면 0회 거래한 내용이나 1회 거래한 내용이나 동일하게 1회로 출력이 된다는 점이었다.

위 부분을 해결하는데 어려움이 있었지만, 강사님의 도움을 통해 해결할 수 있었다.

```sql
SELECT A.ACCOUNT_ID "계좌",
COUNT(T.ACCOUNT_ID) "거래 횟수"
FROM ACCOUNTS A
LEFT OUTER JOIN TRANSACTIONS T
ON A.ACCOUNT_ID = T.ACCOUNT_ID
GROUP BY A.ACCOUNT_ID
ORDER BY COUNT(T.ACCOUNT_ID);
```

위와 같이 거래 내역이 없는 NULL을 제외하기 위해서는 COUNT() 함수를 사용하는 과정에서 OUTER로 붙는 테이블의 한 컬럼을 고른 후 COUNT(컬럼명)을 하게 되면 NULL의 경우는 0으로 카운트되어 출력되어 1회 거래한 내용과 구분이 가능하게 된다.
