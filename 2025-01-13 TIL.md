# TIL: 데이터베이스, 데이터 모델링, ERD

학습일자: 2025년 01월 13일

## 학습하는데 어떤 문제에 부딪혔는지

ERD를 구성하는 요소에 대한 이해가 부족해 구현하는데 어려움이 있었다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

강의를 들은 후 복습을 했다.

## 어떻게 그 문제를 해결 했는지

복습과정에서 한번 더 정리를 하여 이해하고자 노력했다.

## 오늘 내가 새롭게 배운 것은 무엇인지

### 데이터베이스 구성 요소

- 데이터베이스 스키마(어떻게 생겼는지): 데이터베이스의 구조와 제약조건 정의
→ 어떤 column으로 구성, column의 속성, NOTNULL, default값…
- 테이블: 데이터를 저장하는 기본 단위
- 열(Column)과 행(Row): Column = 데이터의 속성 / Row = 데이터의 항목
- 키(Key): Primary Key, Foreign Key
(FK는 다른 테이블의 PK 참조, JOIN할 때 등 사용, 설계할 때는 정의는 하지만, CREATE할 때는 생성을 잘 안함. 이론적으로는 만들어야 하지만, PK에 대한 옵션값 설정 관련 자식 테이블에 존재하는 거 부모테이블에서 삭제하는 등 에러가 나는 경우가 있음. ⇒ FK는 주석화 및 참조용) 등 데이터 간 관계 정의

### 모델링 주요 개념

- 엔티티(Entity): 데이터베이스에서 관리하려는 대상 → DB에서의 테이블
ex) 고객(Customer), 제품(Product), 주문(Order)
- 속성(Attribute): 엔터티의 세부 정보 → DB에서의 컬럼
ex) 고객 엔터티의 이름, 이메일, 연락처
- 관계(Relationship): 엔터티 간의 연결
ex) 고객이 주문을 한다(Customer - Order)
- 키(Key): 데이터를 식별하거나 연결하기 위한 고유의 값
Primary Key: 테이블의 각 행을 고유하게 식별 → 불변성(되도록 변하지 않는다), UNIQUE, NOT NULL, 최소성(PK가 컬럼 하나로 구성될 수 도 있지만, 여러 개의 컬럼을 통해 구성해야 하는 경우 - 오더마스터테이블 / 오더디테일테이블 …) 디테일의 PK는 주문번호가 될 수 없음 하나당 여러줄이 들어가므로. → 오더 + 상품번호의 결합으로 PK / 식별자를 이룸 ⇒ 복합키. 3 ~ 4개까지 가야 식별이 가능하면 인조식별자를 두어 하나로 설계하는 것이 좋음.
Foreign Key: 다른 테이블과의 관계를 정의
- 제약 조건(Constraint): 데이터의 무결성을 유지하기 위한 규칙
ex) UNIQUE, NOT NULL, CHECK

cf) PK와 Unique Key의 차이점 - UK는 NULL 가능, PK는 NOT NULL (= Unique Key + NOT NULL)

### ERD 작성 순서

1. 엔터티를 작성하고 그린다.
2. 엔터티를 적절하게 배치한다.
3. 엔터티 간의 관계를 설정한다
4. 관계명을 기입한다
5. 관계의 참여도를 기입한다 (1대 1, 1대 다…)
6. 관계의 필수/선택 여부를 기입한다.
