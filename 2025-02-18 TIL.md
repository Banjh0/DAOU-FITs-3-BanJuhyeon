# TIL: 쿠키와 세션

학습일자: 2025년 02월 18일

## 학습하는데 어떤 문제에 부딪혔는지

쿠키와 세션에 대해 배웠다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

강의를 열심히 들었다.

## 어떻게 그 문제를 해결 했는지

복습을 했다.

## 오늘 내가 새롭게 배운 것은 무엇인지

HTTP protocol은 Stateless protocol인데, 서버가 클라이언트의 존재를 확인할 수 없다는 것을 의미한다. 즉, 이전에 이 클라이언트가 무슨 요청을 보냈는지 다른 클라이언트와 구분하는 기능을 제공하지 못한다는 것이다.

이에 사용하는 2가지 방법이 쿠키와 세션이다.

1. cookie
    1. cookie는 문자열. key와 value의 쌍으로 여러개의 변수, 값이 문자열형태로 저장된다. 여기에 더해 유효기간, 유효Domain 또한 저장된다.
    2. cookie는 client web browser에 저장됨. 즉, server side가 아닌 client side에 저장된다. 
    3. 추후 동일한 Domain을 가진 서버에 Request를 보내면 이 때 Domain이 동일한 쿠키는 자동으로 같이 넘어간다.
    4. 그래서 doGet()이나 doPost()에서 setCookie라는 메소드를 통해 브라우저에 쿠키 정보를 심고, 이를 확인할 수 도 있다. 
    5. 단, cookie는 client에 저장되기 때문에 보안성이 없다. 사용자가 cookie를 보고, 수정, 삭제가 가능하기에 cookie에는 중요한 데이터를 저장할 수 없다.
    6. 길이 제한이 있어 많은 양의 데이터로 쿠키에 저장할 수 없다.
2. session 이용.
    1. cookie와 다르게 HttpSession이라는 class의 instance. 즉, Heap에 저장되어 있는 객체가 WAS, 즉 server side에 저장된다. 1 클라이언트당 1개만 할당된다.
    2. 서버에 있기 때문에, 상대적으로 많은 양의 데이터, 조금 더 보안성이 있는 데이터 저장 가능하다.
    3. 추후 이전에 접속한 적 있는 클라이언트가 request를 보내면 이전에 저장한 공간을 계속 사용할 수 있게 해준다.
    4. 다만 세션도 쿠키 형태로 저장된 sessionId를 지워버리면 문제가 된다.
