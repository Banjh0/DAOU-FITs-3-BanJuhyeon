# 실습과제 6-1
```C
#define _CRT_SECURE_NO_WARNING // scanf의 오류 원인 제거.
#include <stdio.h>

int main() {
	char gender;
	int age;
	float height;
	printf("성별은?(남자라면 M 여자라면 F) >> ");
	scanf_s("%c", &gender);
	getchar(); // 엔터값 받기
	printf("나이는? >> ");
	scanf_s("%d", &age);
	getchar(); // 엔터값 받기
	printf("키는? >> ");
	scanf_s("%f", &height);
	getchar();
	printf("\n==============\n");
	printf("성별 : %c\n", gender);
	printf("나이 : %3d\n", age);
	printf("키 : %.1f\n", height);

	return 0;
}
```

# 실습과제 6-2
```C
#include <stdio.h>

int main() {
	char name[10]; // 배열은 같은 자료형을 연속 기억공간 저장한 것
	// 배열명은 배열의 시작주소이다.
	printf("이름은? ");
	// 문자열 : 한글자 이상 문자열 "a\0" char name[10]; -> Java와 다르게 String 클래스가 없어 배열로 받음.
	//gets(name);
	//puts(name);
	scanf_s("%s", name, 10);
	getchar(); 
	printf("%s", name);

	char gender;
	printf("\n성별은 M, F로 입력해볼래요? >> ");
	// scanf_s("%c", &gender); // 이거 대신,
	// printf("%c", gender);
	gender = getchar();
	putchar(gender);

	return 0;
}
```

# 실습과제 6-3
```C
#include <stdio.h>

int main() {

	int num1, num2, num3, num4, num5;

	// 여러 개의 자료를 입력하기
	printf("두개의 정수 입력 >> ");
	scanf_s("%d %d", &num1, &num2);
	printf("%d %d", num1, num2);

	printf("\n세개의 정수 입력 >> ");
	scanf_s("%d %d %d", &num3, &num4, &num5);
	printf("%d %d %d", num3, num4, num5);

	return 0;
}
```
