# TIL: SQL 실습

학습일자: 2025년 01월 21일

## 학습하는데 어떤 문제에 부딪혔는지

SQL 실습 문제를 푸는 과정에서 오류가 발생하였다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

배운 내용을 복습하고, 여러 가능성을 대입해 보았다.

## 어떻게 그 문제를 해결 했는지

팀원의 도움을 통해 해당 문제를 해결할 수 있었다.

## 오늘 내가 새롭게 배운 것은 무엇인지

직원들의 입사 연도별 총 급여와 해당 연도의 누적 급여를 계산하는 문제에서, 

```sql
SELECT T."입사 연도", T."해당 연도 총 급여", T."누적 급여"
FROM (
    SELECT TO_CHAR(HIRE_DATE, 'YYYY') "입사 연도", 
        SUM(SALARY) OVER (PARTITION BY TO_CHAR(HIRE_DATE, 'YYYY') ORDER BY HIRE_DATE) "해당 연도 총 급여", 
        SUM(SALARY) OVER (ORDER BY HIRE_DATE) "누적 급여"
    FROM EMPLOYEES
    WHERE SALARY IS NOT NULL
    GROUP BY TO_CHAR(HIRE_DATE, 'YYYY')
) T
;
```

위와 같이 문제를 풀려고 했지만, 이렇게 작성하면 중복된 출력이 많이 나왔다.

하지만, 문제에서는 중복된 데이터가 아닌, 한 해에 해당 연도 총 급여와 누적 급여만을 출력하기를 요청했다.

이를 팀원들의 도움으로 아래와 같이 수정할 수 있었다.

```sql
SELECT DISTINCT TO_CHAR(HIRE_DATE, 'YYYY') "입사 연도", 
    SUM(SALARY) OVER (PARTITION BY TO_CHAR(HIRE_DATE, 'YYYY')) "해당 연도 총 급여", 
    SUM(SALARY) OVER (ORDER BY TO_CHAR(HIRE_DATE, 'YYYY')) "누적 급여"
FROM EMPLOYEES
WHERE SALARY IS NOT NULL
;
```

우선 

1. `SUM(SALARY) OVER (PARTITION BY TO_CHAR(HIRE_DATE, 'YYYY') ORDER BY HIRE_DATE)`  부분에서, `ORDER BY` 를 제거하여 모두 동일한  누적값을 출력하도록 했다. 
2. `SUM(SALARY) OVER (ORDER BY TO_CHAR(HIRE_DATE, 'YYYY'))` 를 통해 누적급여를 연도별로 정확하게 지정해 기존의 `HIRE_DATE` 보다 정확하게 출력할 수 있었다.
3. 이를 통해 출력값이 필요한 부분이 중복되게 표현되게 되었고, `DISTINCT` 를 통해 중복을 제거해 원하는 답을 얻을 수 있었다.

추가적으로, 

```sql
SELECT EXTRACT(YEAR FROM HIRE_DATE) AS YEAR,
        SUM(SALARY),
        SUM(SUM(SALARY)) OVER(ORDER BY EXTRACT(YEAR FROM HIRE_DATE)) AS ACC_SUM
FROM EMPLOYEES
GROUP BY EXTRACT(YEAR FROM HIRE_DATE);
```

위와 같이 총 급여와 누적급여를 SUM 함수만을 이용해 구현하는 방법도 알 수 있었다.
