# TIL: 문자열과 scanf()

학습일자: 2025년 03월 19일

## 학습하는데 어떤 문제에 부딪혔는지

실습문제를 진행하는데, 문자열을 입력받는 일이 많다. 이 때, 문자열 입력은 보통 scanf() 혹은 gets()를 활용하는데 오류가 발생하는 일이 있어 좋은 방법을 찾아보고자 했다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

일단 gets()와 scanf(), 그리고 강의시간에 배운 fgets()를 이용해보았다.

## 어떻게 그 문제를 해결 했는지

인터넷 검색을 통해 새로운 scanf()의 활용법을 배웠다.

## 오늘 내가 새롭게 배운 것은 무엇인지

```c
printf("고객 이름: ");
getchar(); // 버퍼 비우기
fgets(new_transaction->customer_name, MAX_NAME_LEN, stdin);
new_transaction->customer_name[strcspn(new_transaction->customer_name, "\n")] = '\0';

```

기존에 사용하던 코드의 일부이다. fgets()를 통해 문자열을 받고, 이를 strcspn()을 통해 마지막에 붙은 개행문자를 제거하는 모습을 확인할 수 있다.

```c
printf("고객 이름: ");
getchar(); // 버퍼 비우기
scanf("%[^\n]%*c", new_transaction->customer_name);

```

하지만, 이를 scanf()를 이용해 `\n` 이전까지 읽어들이도록 한다면, 조금 더 깔끔하게 코드 작성이 가능해 보였다. 

중요한 것은 다양한 함수에 익숙해진다면 상황에 맞는 입력방법을 적용할 수 있다는 사실이다. 익숙해질 수 있도록 해야겠다.
