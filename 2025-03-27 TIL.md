# TIL: cJSON 라이브러리 사용하기

학습일자: 2025년 03월 27일

## 학습하는데 어떤 문제에 부딪혔는지

C언어를 활용하는 프로젝트에서 JSON으로 데이터를 내보내려는데, `fprintf()` 함수를 이용하는 것은 번거로워서 관련된 라이브러리를 찾고자 했다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

인터넷에서 자료를 찾았다.

## 어떻게 그 문제를 해결 했는지

cJSON이란 라이브러리를 사용하게 되었다.

## 오늘 내가 새롭게 배운 것은 무엇인지

설치를 위해서는 여러 방법이 있지만, 단순하게 공식 cJSON GitHub 페이지에서 cJSON.c과 cJSON.h 파일을 다운받아 프로젝트 솔루션에 추가한다.

```c
#include <stdio.h>
#include <stdlib.h>
#include "cJSON.h"

void save_json_to_file(const char *filename) {
    // JSON 객체 생성
    cJSON *root = cJSON_CreateObject();

    // 키-값 추가
    cJSON_AddStringToObject(root, "name", "Samsung");
    cJSON_AddNumberToObject(root, "price", 75000);
    cJSON_AddNumberToObject(root, "volume", 1200);
    cJSON_AddStringToObject(root, "date", "2025-03-27");

    // JSON 객체를 문자열로 변환
    char *json_string = cJSON_Print(root);

    // 파일에 저장
    FILE *file = fopen(filename, "w");
    if (file) {
        fprintf(file, "%s", json_string);
        fclose(file);
        printf("JSON 파일 저장 완료: %s\n", filename);
    } else {
        printf("파일 열기 실패!\n");
    }

    // 메모리 해제
    cJSON_free(json_string);
    cJSON_Delete(root);
}

int main() {
    save_json_to_file("stock.json");
    return 0;
}

```

위와 같은 예시를 통해 확인할 수 있는 것은,

1. `cJSON_CreateObject()`를 사용하여 JSON 객체를 생성
2. `cJSON_AddStringToObject()`, `cJSON_AddNumberToObject()` 등을 사용하여 키-값 추가
3. `cJSON_Print()`을 사용하여 JSON을 문자열로 변환
4. 파일(`stock.json`)에 저장
5. `cJSON_Delete()`로 JSON 객체 메모리 해제

의 순서로 만들 수 있다는 것을 확인할 수 있다.
