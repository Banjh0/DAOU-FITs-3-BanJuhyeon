# TIL: 정규화

학습일자: 2025년 01월 14일

## 학습하는데 어떤 문제에 부딪혔는지

강의시간 중 풀었던 정규화 관련 문제가 어렵게 느껴졌다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

우선, 배운 정규화 관련 부분을 정리하고 차근차근 단계적으로 문제를 해결해나가고자 노력했다.

## 어떻게 그 문제를 해결 했는지

동기들과 강사님의 설명을 다시 듣고, 문제를 풀었다.

## 오늘 내가 새롭게 배운 것은 무엇인지

## 정규화(Normalization)

- 데이터베이스의 **설계 단계에서 데이터 중복을 제거**하고 데이터 무결성을 유지하기 위한 과정
⇒ 중복이 많으면 무결성이나 일관성을 유지하기 어려움.
- 데이터를 논리적으로 분리하여 효율적으로 저장하고 관리하도록 설계
- 데이터 중복 방지
- 데이터 일관성 및 무결성 유지
- 데이터 저장소의 효율성 향상
- 데이터 변경 시 발생할 수 있는 문제(삽입, 삭제, 갱신 이상) 최소화

→ 데이터가 꼬이는 것을 막기 위해 테이블을 잘게 쪼개는 과정.

- 정규형은 특징에 맞는 것, 정규화는 그렇지 못하는 것을 정규형으로 만들고자 하는 것.

### 제1정규형(1NF)

- 모든 속성은 반드시 하나의 값만 가져야 한다
직업에 배우, 가수, 작곡가와 같이 여러개 있으면 SELECT 하기 위해 LIKE 검색을 해야하며, 이는 EQUAL 검색보다 효율성이 떨어짐.
- 유사한 속성이 반복되는 경우도 1차 정규화의 대상
ex) 고객들이 SNS 사이트를 운영하는지 저장할 때, 컬럼을 여러개 만들어 두면 NULL값이 있는 컬럼이 존재하거나, 컬럼을 추가하는 상황 발생 가능. 번거롭고 비효율적이므로 사이트 엔터티를 따로 떼어낼 수 있음.
- 실제로 모델링한 사례 → 이메일 인증, 휴대폰 본인인증에서 SNS로 회원 가입이 생겨남. SNS를 여러개 연동할 수 있는데, 제1정규형으로 설계하면 SNS로그인 데이터는 별도 테이블로.

### 제2정규형(2NF) ⇒ 제1정규형은 만족했다는 전제

- 엔터티의 모든 일반속성은 반드시 모든 주식별자에 종속되어야 한다
- 일반속성이란? 주식별자 아닌 나머지 속성들. 뒷쪽에 위치.
- 일반속성인 음료명이 주식별자 중 음료코드 속성에만 종속되면 문제가 무엇인가?
→ 새로운 음료 메뉴를 넣으면 주문된 적 없으면 주문번호가 NULL, 주문번호는 PK이기 때문에 NOT NULL ⇒ 삽입 이상
→ 메뉴를 지울 때도 주문건을 만들어줘야 하므로 삭제를 할 수 없는 삭제 이상
→ 업데이트 할 때 음료코드와 음료명을 변경하려면 그동안 주문했던 모든 row를 다 바꿔야 함. ⇒ 갱신이상
그래서 데이터를 따로 떼어 음료 마스터(상품 마스터성 테이블)을 떼어내 엔터티를 분리.

### 제3정규형(3NF) ⇒ 1~2정규형은 만족했다는 전제

- 주식별자가 아닌 모든 속성 간에는 서로 종속될 수 없다
- 개념은 제2정규형과 비슷.
- 다른 속성에 종속되면 삽입이상, 삭제이상, 갱신이상 등이 생길 수 있음.

문제에서는, 우선 제1정규화를 통해 LAB실 관련된 부분을 따로 엔터티로 분리한다.

이후, 복합키가 없으므로 제2정규형은 건너뛰고, 제3정규형의 경우 주식별자가 아닌 모든 속성 간에는 서로 종속될 수 없다는 원칙 아래 교수명과 교수번호를 따로 엔터티로 분리하고, 학생엔터티에서 학과 관련 엔터티도 따로 뺀다.

대출자 번호와 대출자명도 따로 엔터티로 분리할 수 있지만, 대출자신분구분이 있으므로 학생 / 교수 테이블을 참조하게 만든다.

도서번호와 도서명 관련도 종속되므로, 대출 도서를 정규화 하여 도서 마스터성 엔터티로 따로 만든다.

마지막으로, 각 도서는 동일한 대출번호를 가져도 각각 반납이 가능하므로, 대출관련 엔터티를 따로 만들어 반납일자를 위치시켜 총 7개의 엔터티로 분리할 수 있다.
