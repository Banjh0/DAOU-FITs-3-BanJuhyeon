# TIL: 컴퓨터통신과 네트워킹

학습일자: 2025년 01월 10일

## 학습하는데 어떤 문제에 부딪혔는지

컴퓨터 통신과 네트워킹 분야를 처음 배우기에 걱정이 앞섰다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

금일 강의를 시작하기에 앞서, 한 번 전체 내용을 훑어보았다.

## 어떻게 그 문제를 해결 했는지

강의를 열심히 들었다.

## 오늘 내가 새롭게 배운 것은 무엇인지

네트워크의 계층구조를 정리해보았다.

네트워크의 계층구조를 나눈데에는 여러 기준이 있지만, 크게 5단계로 나누었다.

1. **물리 계층**
    - 실제 전송 매체에서 데이터를 전기적 신호나 광신호로 전송하는 프로토콜
    - 전송 매체: 신호를 한쪽에서 다른 쪽으로 전달하는데 사용되는 물질
        - 무선 전송매체 : 공기
        - 유선 전송매체 : 통신선
    - 통신선 (Communication Line)
        - 선이 길어질수록 신호의 세기가 감소
        - 신호가 외부의 힘에 간섭을 받음
        - 꼬임선 (Twisted Pair): 두 개의 전선을 꼬아 외부의 영향을 덜 받게 함 ex) 이더넷 단자
        - 동축선 (Coaxial Cable): 중앙에 구리선이 있고 이를 내부 피복으로 감싼 구조 ex) BNC 단자
        - 광섬유 (Optical Fiber): 머리카락보다 가느다란 선에 빛을 이용하여 데이터를 전송, 빛을 이용하기 때문에 도청이 어려움
        - 주요 통신선 비교
            
            
            | 선 | 대역폭 | 전송 거리 | 외부 간섭에 강한 정도 | 도청 |
            | --- | --- | --- | --- | --- |
            | 꼬임선 | 10 ~ 100Mbps | 100m ~ 1km | 약함 | 가능 |
            | 동축선 | 10Mbps ~ 1Gbps | 1 ~ 10km | 꼬임선보다 강하고 광섬유보다 약함 | 가능 |
            | 광섬유 | 2 ~ 200Gbps | 10 ~ 100km | 강함 | 불가능 |

2. **데이터 링크 계층**
    - 같은 네트워크 내에서 패킷을 물리적 주소로 전송
        - 프레임(frame) : 데이터 링크 헤더 + 패킷
    - IP주소는 실질적인 주소보다 논리적인 주소, 물리적인 주소는 MAC 주소
    - 전송방식
        - Ethernet
            - 유선 LAN에서 널리 사용되는 데이터 링크 프로토콜
            - MAC 주소를 사용해 네트워크 장치 간에 데이터를 전송
        - WiFi (IEEE 802.11)
            - 무선 네트워크에서 사용되는 프로토콜
            - Wi-Fi를 통해 장치 간에 데이터를 전송
    - MAC 주소
        - 데이터 링크 계층에서 사용되는 물리적 주소
        - 네트워크 인터페이스 카드(NIC)나 네트워크 장치에 고유하게 할당
        - 48비트 길이로 12자리의 16진수로 표현. 첫 6자리는 제조사 식별자, 나머지 6자리는 각 장치에 고유한 식별 번호 ex) 00:1A:2B:3C:4D:5E
    - 오류를 처리하는 방식
        - 데이터 중복 전송: 송신측에서 수신측으로 각 데이터를 두 번씩 보내는 방법
        - 중복 정보 전송: 전송할 데이터에 대한 중복 정보(ex. 패리티 비트)를 데이터와 함께 보내는 방법
    - 오류 처리 코드
        
        
        | 방식 | 종류 | 설명 |
        | --- | --- | --- |
        | 오류 탐색 코드 | 패리티 비트, 체크섬, CRC 코드 | 오류를 찾는다 |
        | 오류 보정 코드 | 허밍 코드 | 오류를 찾고 보정한다. 오버헤드가 크다 |
    - **수직 중복 검사 (VRC, Vertical Redundancy Check)**
        - 데이터에 대한 패리티 비트라는 중복 정보를 구해 데이터와 함께 수신측으로 전송
        - **패리티 비트 (Parity Bit)**
            - 보내려는 데이터에 1비트를 추가하여 전체 1의 개수가 짝수 혹은 홀수가 되도록 만드는 방식
            - 오류가 짝수 개 발생하면 오류를 찾지 못함 
            → 네트워크에서 발생한 오류는 연속 오류일 가능성이 높음. 이는 치명적인 단점
            - 패리티 비트 1비트를 추가할 때 마다 1개의 오류를 더 찾음
            - 짝수 패리티 비트는 1의 개수가 짝수, 홀수 패리티 비트는 1의 개수가 홀수로 전송
        - 짝수 패리티 전송 예시)
            - 짝수 패리티 비트를 데이터와 함께 수신측으로 전송
            - 수신측에서는 수신한 정보(데이터와 패리티 비트 모두)에 대한 패리티 비트가 모두 0이면 오류가 발생하지 않은 것으로 판단, 패리티 비트가 1이면 오류가 발생한 것으로 판단
        - 문제점
            - 짝수 개의 비트에 오류가 발생하면 오류를 검출할 수 없음
    - **세로 중복 검사 (LRC, Logitudinal Redundancy Check)**
        - 데이터를 행렬의 형태로 구성해 오류 검출
        - 세로 중복 검사 과정
            - 데이터를 8비트로 구분해 3행 8열의 행렬로 변환한 다음, 같은 열에 대응되는 비트들에 대한 패리티 비트를 구함
            - 중복 정보를 포함해 데이터 전송
            - 수신측에서 오류 검사
    - **순환 중복 검사 (CRC, Cyclic Redundancy Check)**
        - 숫자를 추가한 데이터를 CRC 코드 값으로 나누었을 때 0이 되도록 함. XOR 연산(같으면 0, 다르면 1)
        - 가장 널리 사용하는 오류 검출 방법
        - 순환 중복 검사 과정
            - 모듈로 -2 나눗셈 결과 나머지를 구해 중복 정보로 만듬
            - 계산된 중복 정보를 데이터와 함께 전송
            - 수신측에서는 전송받은 정보를 동일한 n + 1 제수로 모듈로 -2 나눗셈을 실시하여 나머지가 0이면 오류가 없는 것으로 판단, 나머지가 0이 아니면 오류로 간주.
    - **체크섬 (Checksum)**
        - 보내려는 데이터를 일정 크기로 자르고 이를 모두 더하여 코드를 만듬
        - 송신측의 TCP(또는 UDP)에서 전송할 데이터에 대한 체크섬을 구해 TCP 헤더에 포함해서 전송
        - 수신측의 TCP에서 TCP헤더에 포함된 체크섬을 검사해 수신한 데이터에 오류가 있는지 확인
    - 오류 처리 코드 비교
        - 패리티 비트 vs CRC 코드
            - CRC 코드는 적은 오버헤드로 많은 수의 오류를 찾을 수 있어 강력함
            - 예를 들어, 8비트를 추가할 때 패리티 비트는 최대 8개, CRC 코드는 최대 $2^8 - 1$개의 오류를 찾을 수 있음
        - CRC 코드 vs 체크섬
            - 코드로 나눈 나머지 또는 합이 다를 때 오류
            - 체크섬이 찾을 수 있는 오류의 개수는 CRC 코드보다 적음
            - 체크섬은 우연히 변형된 값이 상쇄되어 합이 같으면 오류를 찾을 수 없음

3. **네트워크 계층**
    - 데이터를 목적지까지 라우팅하고 전달하는 역할
        - **라우팅** (routing): 어떤 길로 전달하는 역할
        - 라우터 (router): 패킷을 어느 쪽 네트워크로 보낼지 결정하는 통신장비
        - 네트워크 장치(라우터)는 네트워크 간에 패킷을 전달하는데 사용
    - **IP 주소**
        - 인터넷에 연결된 여러 장치를 식별하는 번호.
        - 네 개의 10진수로 구성. 각 10진수는 여덟자리 2진수를 의미
        - 4바이트, 32비트로 이뤄진 체계
        - 0.0.0.0 부터 255.255.255.255까지 총 2의 32승 만큼의 주소가 가능
    - IP 주소와 MAC 주소
        - IP 주소는 네트워크를 통해 장치를 찾고, MAC 주소는 로컬 네트워크에서 해당 장치와 직접 통신
    - IP 주소의 구분
        - 네트워크 주소: 해당 장치가 속해 있는 네트워크를 식별
        - 호스트 주소: 해당 네트워크 내에서 해당 장치를 식별
        - 한 네트워크에 포함된 모든 장치들은 같은 네트워크 주소를 갖지만, 한 네트워크 내의 호스트 주소는 모두 다름
    - 클래스
        - 주어진 IP 주소의 첫번째 자리로 A, B, C, D, E의 다섯 가지 클래스로 구분. 클래스 D와 E는 특수한 목적으로 사용
        - 클래스별 주소 범위
            - 클래스 A: 0.0.0.0 ~ 127.255.255.255
            - 클래스 B: 128.0.0.0 ~ 191.255.255.255
            - 클래스 C: 192.0.0.0 ~ 223.255.255.255
        - 127.0.0.1 → 자기가 사용하는 LAN 카드 자신. 루프백 주소라고도함
    - 서브넷 마스크 (Subnet Mask)
        - IP 네트워크에서 서브넷을 나누기 위해 사용
        - 32비트 이진 숫자로 구성되며, IP 주소와 유사한 형식
        - 네트워크 주소와 호스트 주소를 구분
            - 네트워크 주소: 서브넷 마스크에서 1로 설정
            - 호스트 주소: 서브넷 마스크에서 0으로 설정. 해당 네트워크 내에서 호스트(컴퓨터, 라우터 등)를 구분
        - 255.255.255.0 → 11111111.11111111.11111111.00000000 (32비트)
            - 처음 24비트가 네트워크 주소, 나머지 8비트가 호스트 주소
            - IP 주소가 192.168.1.10 인 경우, 192.168.1.0가 네트워크 주소
        - 예시
        - 네트워크 주소가 처음 24비트인 경우 (/24 서브넷 마스크)
            - IP 주소: 192.168.1.10
            - 서브넷 마스크: 255.255.255.0
            - 네트워크 주소: 192.168.1.0
            - 호스트 주소: 192.168.1.10
        - 네트워크 주소가 처음 16비트인 경우 (/16 서브넷 마스크)
            - IP 주소: 192.168.1.10
            - 서브넷 마스크: 255.255.0.0
            - 네트워크 주소: 192.168.0.0
            - 호스트 주소: 192.168.1.10
    - IPv4와 IPv6
        - IPv4 (Internet Protocol version 4)
            - 32비트 주소 체계 사용
            - 0.0.0.0부터 255.255.255.255까지의 범위에서 주소를 할당할 수 있음
            - 기기가 움직이면 지역 코드가 변경되어 IP 주소를 수시로 변경해야 하는 등 주소 부족 문제가 발생할 수 있음
            - ex) 182.168.1.1
        - IPv6 (Internet Protocol version 6)
            - 128비트 주소 체계 사용
            - 훨씬 더 많은 고유 주소를 제공하여 주소 부족 문제를 해결할 수 있음
            - ex) 2001:0db8:85a3:0000:0000:8a2e:0370:7334
    - IP 헤더
        - 헤더의 크기: 20 바이트
    - MAC 헤더
        - 데이터를 보낼 상대의 IP 주소가 정해지면, 상대 네트워크 카드의 MAC 주소를 포함하고 있는 MAC 헤더를 추가
        - 헤더의 크기: 14바이트
        - ARP (Address Resolution Protocol): 수신측의 MAC주소를 구할 대 사용하는 프로토콜
    - 라우팅 테이블
        - 데이터를 어디로 보낼지 나타낸 표
        - 라우팅 테이블 보는 방법
            - 목적지 주소가 111.111.111.3일 때,
                1. 넷마스크가 1로 되어 있는 수신 네트워크 주소 추출
                2. 목적지 주소와 추출한 수신 네트워크 주소가 같은 지 비교
                3. 같으면 해당 행의 게이트웨이로 데이터 전송
            - 목적지 주소가 222.222.222.3일 때,
                1. 라우팅 테이블에서 적용되는 행이 없을 때에는 최종적으로 첫 번째 행 적용
                2. 해당 행의 게이트웨이로 데이터 전송
    - 게이트웨이 (Gateway)
        - 컴퓨터 네트워크에서 다른 네트워크와 연결되도록 도와주는 장치로 네트워크 간의 다리 역할을 함
        - 라우터는 주로 같은 네트워크 내의 데이터를 관리하고, 게이트웨이는 다른 네트워크와 연결하는 기능
        - 기본 게이트웨이 동작 예
            - 내부 네트워크: 192.168.1.0/24
            - 기본 게이트웨이: 192.168.1.1 (라우터) → 목적지: 8.8.8.8 (구글의 DNS 서버)
    - 허브 (Hub)
        - 컴퓨터 네트워크에서 여러 장치를 연결하는 데 사용되는 네트워크 장비로 단순한 재전송 장치
        - 네트워크에 연결된 장치들 간에 데이터를 모든 장치로 전달
        - 주로 소규모 네트워크나 매우 기본적인 용도로만 사용
    - 스위치 (Switch)
        - 허브보다는 advanced한 성능을 가짐. 프레임을 전송할 때 트래픽도 관리를 해줌. 허브가 한번에 한 노드만이 보낼 수 있다면, 스위치는 여러개의 노드에서 동시통신이 가능.
        - 네트워크 장치들 간에 데이터 프레임을 전송하는 지능형 장비
        - 데이터를 필요한 장치로만 전송
        - 네트워크 트래픽을 효율적으로 관리
        - 대규모 네트워크와 고급 네트워크 환경에 적합
    - 동적 라우팅 프로토콜
        - RIP (Routing Information Protocol) → (AS)시스템 안에서 라우팅
            - 거리 벡터(distance vector) 기반의 라우팅 프로토콜
            - 목적지까지의 거리를 홉 수(hop count)로 측정. 최대 홉 수를 15로 제한
            - 거리 벡터를 계산해 가장 짧은 거리로 보내주는 방법.
            - 거리를 찾을 때 노드와 노드 사이를 홉이라고 지칭. 홉을 다 계산한 후 홉의 갯수가 가장 작은 곳으로 가는 방법
        - OSPF (Open Shortest Path First) → (AS)시스템 안에서 라우팅
            - 링크 상대 (link state) 라우팅 프로토콜로 계층적 라우팅을 지원
            - 네트워크의 각 라우터가 자신의 상태 정보를 전체 네트워크에 전파하여 최적의 경로를 계산
            - 라우팅을 할 때 링크 스테이트 정보를 가지고 라우팅을 함. 현재 링크된 상태를 보고 최적 경로를 해줌.
        - BGP (Border Gateway Protocol) → (AS)시스템 밖에서 라우팅
            - 경계 라우팅 프로토콜로 핵심 라우팅 프로토콜
            - 서로 다른 Autonomous System(AS → 현재 가지고 있는 시스템의 바운더리) 간에 라우팅 정보를 교환
            - 경로 벡터(path vector)를 이용하여 네트워크 간의 데이터 경로를 설정
    - IP 주소 유형
        - 사설 IP 주소 (Private IP Address)
            - 내부 네트워크에서 사용되며, 공인 IP주소와 겹칠 수 없음
            - 사설 IP 주소는 외부 인터넷과 직접 통신할 수 없으며, NAT(Nerwork Address Translation) 기술을 통해 공인 IP로 변환되어 인터넷과 연결
            - ex) 192.168.x.x, 10.x.x.x, 172.16.x.x ~ 172.31.x.x
        - 공인 IP 주소 (Public IP Address)
            - 인터넷에서 고유한 주소로, 전 세계적으로 유일. ISP(인터넷 서비스 제공업체)가 할당
            - ex) 8.8.8.8 (Google의 DNS 서버 주소)
    - DHCP (Dynamic Host Configuration Protocol)
        - 네트워크에서 장치들이 IP 주소 및 기타 필요한 네트워크 설정을 자동으로 할당받을 수 있도록 도와주는 프로토콜
        - 초고속 인터넷 서비스 회사에서 개인의 서버 설치를 막기 위해 IP 주소를 자동으로 할당
    - 도메인 네임 (Domain Name)
        - IP 주소에 이름을 부여한 것.
        - 웹 브라우저의 주소표시줄에 IP주소 대신 도메인 이름을 입력하여 접속 ex) www.hanbit.co.kr
        - 도메인 네임 규칙
            - 마침표로 구분하여 계층화
            - 오른쪽에 있는 이름이 상위 계층
            - 이름. 종류. 국가 순
    - 도메인 네임 시스템 (DNS, Domain Name System)
        - 도메인 네임과 IP 주소를 테이블 형태로 모아놓은 서버
        - 도메인 이름에 대한 IP 주소를 등록하고 있으면서 도메인 이름에 대한 IP 주소를 알려주거나 IP 주소에 대한 도메인 이름을 알려주는 시스템
    - DNS 서버의 계층 구조
        - 하위 계층의 도메인 이름에 대한 정보를 바로 위의 상위 계층의 DNS 서버에서 등록하여 관리함
        - 로컬DNS → 루트 DNS → 국가 DNS → 지역 DNS → IP 주소 반환

4. **전송 계층**
    - 데이터 전송의 정확성과 순서를 보장하는 계층
    - 네트워크 계층에서 올라온 데이터를 정리하고 데이터를 프로그램에 전달
    - **TCP는 일련번호를 매겨 패킷을 전송 → 패킷 누락, 순서 뒤바뀜방지**
    - **전송과정**
    : 데이터를 잘라 패킷으로 만들고 → IP를 이용해 라우터를 건너 도착 → 패킷을 다시 재조립해 원래 데이터를 확인
    - TCP (Transmission Control Protocol)
        - 송신측과 수신측의 연결이 이루어진 것을 확인한 다음에 데이터를 전송하는 프로토콜 신뢰성이 요구되는 데이터 전송 시 사용
        - 전송시 데이터는 TCP 계층을 거쳐 IP 계층으로 전달됨
    - TCP 헤더
        - 헤더의 크기: 20바이트
        - TCP 헤더의 제어 비트
            - SYN 비트를 1로 하면 연결을 요청, ACK 비트를 1로 하면 ‘응’ 대답
        - TCP 헤더의 순서번호와 인정번호
            - 송신측의 TCP에서 데이터의 일련 번호인 순서 번호를 TCP 헤더에 포함시켜 데이터를 전송
            - 수신측에서는 수신한 데이터의 순서 번호에 받은 데이터의 크기를 더한 값을 인정 번호로 하여 잘 받았다는 응답을 송신측에 전송
    - TCP의 **데이터 분할**
        - TCP는 전송할 데이터가 큰 경우 데이터를 적절한 크기로 분할하여 각각의 분할된 부분에 TCP 헤더를 추가
    - UDP (User Datagram Protocol)
        - 송신측과 수신측의 연결 과정을 거치지 않고 일방적으로 데이터를 보내고 수신측으로부터 응답도 따로 받지 않는 프로토콜
        - 신뢰성이 요구되지는 않고, 간단한 데이터를 빠른 속도로 전송할 때 사용
        - 데이터가 사라지거나 뒤바뀔 수는 있지만, 처리 속도가 매우 빠름
    - **포트 (Port)**
        - 응용 프로그램 구분을 위해 전송 계층이 사용하는 주소
        - 포트번호: 수신측에서 동작하는 여러 애플리케이션 중 데이터를 수신할 애플리케이션을 식별하는 번호
        - HTTP: 80, HTTPS: 443, FTP: 21 & 20, SSH: 22, SMTP: 25

5. **응용계층**
    - 사용자가 직접 상호작용하는 네트워크의 최상위 계층. 사용자 요청을 처리
    - 월드 와이드 웹 (WWW, World Wide Web)
        - 인터넷에서 정보를 쉽게 찾을 수 있도록 제공하는 서비스
        - 하이퍼텍스트(텍스트 정보 연결), 하이퍼미디어(멀티미디어 정보 연결) 방식 이용
        - HTTP (HyperText Transfer Protocol): 웹에서 데이터를 전송하기 위한 비보안 프로토콜
        - HTTPS (HyperText Transfer Protocol Secure):  보안을 추가한 프로토콜. SSL(Secure Sockets Layer) 또는 TLS(Transport Layer Security) 암호화를 사용하여 데이터 전송을 보호
        - 보안소켓층 (SSL, Secure Sockets Layer)
            - 웹클라이언트와 서버 사이의 보안 연결 → 정보를 암호화 → 인터넷으로 정보를 보냄 → 브라우저에 있는 자물쇠 아이콘이나 https의 s를 의미
        - 암호화 (Encryption)
            - 맞는 해독키가 없으면 읽을 수 없도록 파일의 내용을 바꿔놓는 것
        - 공중키 암호화 (Public Key Encryption, PKE)
            - 두 개의 키를 이용하는데, 모든 사람을 위해서 공중키를, 암호화된 정보를 받는 사람을 위해서는 개인키를 사용 → 더 안전하게 암호화해서 데이터를 전송
        - TLS (Transport Layer Security)
            - 인터넷을 통한 통신을 안전하게 보호하는 프로토콜
            - 데이터의 기밀성, 무결성, 인증을 제공하여 인터넷 상의 데이터 전송을 암호화
            - SSL(Secure Sockets Layer)의 후속 프로토콜로, SSL의 보안 취약점을 개선하고 더 강력한 암호화와 보안을 제공
            - 웹 브라우징(HTTPS), 이메일, VoIP 및 기타 인터넷 서비스에서 보안을 강화하는데 사용됨
        - 텔넷 (Telnet)
            - 원격지의 컴퓨터를 인터넷을 통해 접속하여 자신의 컴퓨터처럼 사용할 수 있는 원격 접속 서비스
        - FTP (File Transfer Protocol)
            - 인터넷을 통해 컴퓨터 간 파일 송수신을 위한 프로토콜
            - FTP 서버에게 파일을 업로드하거나 반대로 다운로드 할 수 있음
        - SSH (Secure Shell)
            - 네트워크 상에서 컴퓨터나 서버와 안전하게 통신하고 원격으로 관리할 수 있는 프로토콜
            - 암호화된 연결을 사용하여 주로 보안이 중요한 환경에서 원격 로그인, 파일 전송, 그리고 다른 원격 명령어 실행 등을 위해 사용
            - 원격 서버 관리 → ssh user@server_ip_address
            - 파일 전송 → ssh local_file user@remote_host:/remote/directory
            - 자동화된 스크립트 실행 → ssh user@server_ip ‘bash /path/to/script.sh’
            - 포트 포워딩 및 터널링 → ssh -L 8080:localhost:80 user@remote_host
        - 전자우편 (E-mail)
            - 인터넷을 통해 편지를 교환하는 서비스
                - SMTP (Simple Mail Transfer Protocol): 이메일 전송에 사용되는 프로토콜
                - POP3 (Post Office Protocol 3): 이메일 수신에 사용되는 프로토콜
                - IMAP (Internet Message Accers Protocol): POP3와 비슷하지만, 이메일을 서버에서 읽고 관리하는데 사용하는 프로토콜
        - 방화벽 (Firewall)
            - 네트워크 보안을 강화하기 위해 네트워크 트래픽을 제어하는 장치 또는 시스템
            - 패킷 필터링을 사용하여 트래픽을 검사하고, 설정된 규칙에 따라 허용하거나 차단
            - 침입 탐지 및 차단 시스템 포함하는 등 애플리케이션 계층에서 고급 보호 기능을 제공함
            - 악의적인 공격이나 불법적인 접근으로부터 시스템을 보호
