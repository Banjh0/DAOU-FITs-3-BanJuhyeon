# 실습과제 11-1
```C
#include <stdio.h>

int main() {
	int a, b;

	printf("가감승제를 원하는 두 수를 입력하세요 : ");
	scanf_s("%d %d", &a, &b);
	printf("%d + %d = %d\n", a, b, a + b);
	printf("%d - %d = %d\n", a, b, a - b);
	printf("%d * %d = %d\n", a, b, a * b);
	printf("%d / %d = %lf", a, b, a / (double)b);

	return 0;
}
```

# 실습과제 11-2
```C
#include <stdio.h>
void gugudan(int dansu);

int main() {
	int dansu;

	printf("원하는 구구단 단수를 입력하시오. ");
	scanf_s("%d", &dansu);
	gugudan(dansu);

	return 0;
}

void gugudan(int dansu) {
	for (int i = 1; i <= 9; i++) {
		printf("%d X %d = %d\n", dansu, i, dansu * i);
	}
}
```

# 실습과제 11-3
```C
#include <stdio.h>
#include <string.h>

int main() {
	char string[100];

	printf("문자열을 입력하시오 ");
	gets(string);
	for (int i = 0; i < strlen(string); i++) {
		if (string[i] < 97) {
			string[i] += 32;
		}
		else {
			string[i] -= 32;
		}
	}
	for (int i = 0; i < strlen(string); i++) {
		printf("%c", string[i]);
	}

	return 0;
}
```
