# 실습과제 6

```SQL
-- 승인된 대출(APPROVED)의 총 대출 금액과 해당 대출 건수를 출력하시오.
SELECT SUM(AMOUNT) AS "총 대출 금액", COUNT(*) AS "대출 건수" FROM LOANS WHERE STATUS LIKE 'APPROVED';

-- 각 월별 총 거래 금액을 계산하고, 거래 금액이 10,000 이상인 달만 출력하시오.
SELECT TO_CHAR(TRANSACTION_DATE, 'YYYY-MM') AS "거래 금액이 10,000이상인 달" 
FROM TRANSACTIONS 
GROUP BY TO_CHAR(TRANSACTION_DATE, 'YYYY-MM') 
HAVING SUM(AMOUNT) >= 10000;

-- 각 지점에서 승인된 대출(APPROVED)의 평균 대출 금액과 총 대출 금액을 계산하시오.
SELECT BRANCH_ID, AVG(AMOUNT) AS "승인된 평균 대출 금액", 
    SUM(AMOUNT) AS "승인된 총 대출 금액" 
FROM LOANS 
WHERE STATUS LIKE 'APPROVED' 
GROUP BY BRANCH_ID;
```
