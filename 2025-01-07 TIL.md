# TIL: 알고리즘과 자료구조

학습일자: 2025년 01월 07일

## 학습하는데 어떤 문제에 부딪혔는지

자료구조, 특히 배열, 연결리스트, 큐, 이진검색트리에 대한 개념을 배우는데 어려움을 느꼈다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

개념에 익숙해지기 위해 강의를 열심히 들었다.

## 어떻게 그 문제를 해결 했는지

강의를 들은 후 TIL을 통해 복습하고자 했다.

## 오늘 내가 새롭게 배운 것은 무엇인지

### 배열

- 같은 자료형의 데이터를 순서대로 나열한 구조.
- 인덱스는 첫 번째로부터 떨어진 상대적인 위치, 즉 몇 번째에 있는가를 나타냄.
- 배열에서 올 수 있는 인덱스의 시작 숫자는 0 또는 1이 올 수 있는데, 파이썬에서는 0을 사용
- 1차원 배열
    - 인덱스를 하나만 사용하는 배열
- 2차원 배열
    - 두 개의 인덱스를 사용하는 배열

### 연결 리스트 (Linked List)

- 각 데이터들을 포인터로 연결하여 관리하는 구조 → 연결되어있는 것처럼 보이지만, 내부적으로는 따로따로 존재. 포인터만 연결.
- 각 노드들은 주기억장치의 어느 위치에 저장되든 상관없고, 각 노드들은 포인터로 연결되어 있음
- 데이터 영역에 여러 개의 데이터가 저장될 수 있음
- 요소들이 메모리 상에서 연속적이지 않으며, 각각의 노드는 동적으로 할당되어 연결
- 단순 연결 리스트 (Singly Linked List) : 노드에 포인터 영역이 하나인 연결 리스트
- 이중 연결 리스트 (Doubly Linked List) : 각 노드에 다음 노드를 가리키는 포인터 영역과 이전 노드를 가리키는 포인터 영역을 포함

→ 삭제와 삽입이 많은 경우 Linked List를 사용, 그렇지 않다면 배열을 사용한다.

### 큐 (Queue)

데이터가 한쪽 방향으로 삽입되고 반대 방향으로 삭제되는 구조 
→ 가장 먼저 삽입된 데이터가 가장 먼저 삭제 == 선입선출(FIFO, First-In, First-Out)

- 큐 삽입 (Enqueue)
    - 큐에 새로운 데이터를 추가하는 작업. 삽입된 데이터는 큐의 뒤쪽에 위치하게 됨
    - 동작 방식
        - 큐의 뒤쪽에 데이터를 추가
        - 큐에 데이터가 있을 경우, 삽입된 데이터는 기존 데이터 뒤에 추가
- 큐 삭제 (Dequeue)
    - 큐에서 가장 먼저 들어온 데이터를 제거하는 과정.
    - 동작 방식
        - 큐의 앞쪽에 있는 데이터를 제거
        - 큐가 비어 있을 경우, 삭제 작업을 할 수 없음

### 이진 탐색 트리 (Binary Search Tree)

- 이진 트리 중에서 같은 데이터를 갖는 노드가 없으며, 왼쪽 서브 트리에 있는 모든 데이터가 현재 노드의 데이터보다 작고, 오른쪽 서브 트리에 있는 모든 데이터가 현재 노드의 데이터보다 큰 트리
- 데이터를 추가하기 위해서는 루트 노드부터 시작해, 루트의 값보다 크면 우측, 작으면 좌측으로 이동해 끝에 추가해주면 된다.
- 끝 데이터를 삭제하는 것은 그냥 삭제하지만, 삭제할 노드의 자식 노드가 하나인 경우, 그 자식 노드가 그대로 붙지만 삭제할 노드의 자식 노드가 두개인 경우, 위에 접붙이는 느낌으로 한칸씩 올라감

### 이진 탐색 (Binary Search)

정렬된 데이터 집합을 반으로 쪼개 가면서 탐색하는 방법
+ 이진 탐색을 위한 조건: 배열이 정렬이 되어있어야 함.
