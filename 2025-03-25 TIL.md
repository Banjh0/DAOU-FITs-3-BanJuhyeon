# TIL: OCIBind와 OCIDefine

학습일자: 2025년 03월 25일

## 학습하는데 어떤 문제에 부딪혔는지

복합문제를 푸는 과정에서 `OCIBind`와 `OCIDefine`의 차이점에 대해 의문을 가지게 되었다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

이전 수업 내용의 코드를 살펴보았다.

## 어떻게 그 문제를 해결 했는지

코드 내용을 살펴보고, 관련하여 정리를 해보았다.

## 오늘 내가 새롭게 배운 것은 무엇인지

### 1. `OCIBind` (바인드 변수, 입력 데이터용)

- 목적: SQL 문에서 입력 데이터(바인드 변수)를 설정하는 데 사용됩니다.
- 사용 시점: `OCIStmtPrepare()`로 SQL을 준비한 후, `OCIStmtExecute()`를 실행하기 전에 사용됩니다.
- 주요 함수:
    - `OCIBindByName()` : 바인드 변수를 SQL에서 변수명(`:var_name`)을 기준으로 설정
    - `OCIBindByPos()` : 바인드 변수를 위치(순번) 기준으로 설정

```c
    /*** 데이터 삭제 (DELETE) ***/
    char* delete_sql = "DELETE FROM TEST_CUSTOMER WHERE CUSTOMER_ID = :1";  // ID를 기준으로 삭제
    OCIHandleAlloc(envhp, (void**)&stmthp, OCI_HTYPE_STMT, 0, NULL);
    OCIStmtPrepare(stmthp, errhp, (text*)delete_sql, strlen(delete_sql), OCI_NTV_SYNTAX,
        OCI_DEFAULT);
    int delete_id = 3;  // 삭제할 ID 값
    // 바인딩 변수 설정 (DELETE)
    OCIBindByPos(stmthp, &bnd1, errhp, 1, &delete_id, sizeof(delete_id), SQLT_INT,
        NULL, NULL, NULL, 0, NULL, OCI_DEFAULT);
    // SQL 실행
    if (OCIStmtExecute(svchp, stmthp, errhp, 1, 0, NULL, NULL,
        OCI_COMMIT_ON_SUCCESS) != OCI_SUCCESS) {
        check_error(errhp);
    }
    else {
        printf("데이터 삭제 완료!\n");
    }
```

### 2. `OCIDefine` (결과값 바인딩, 출력 데이터용)

- 목적: SQL 실행 결과를 사용자 버퍼에 저장하는 데 사용됩니다.
- 사용 시점: `OCIStmtExecute()` 이후, `OCIStmtFetch()`로 데이터를 가져오기 전에 사용됩니다.
- 주요 함수:
    - `OCIDefineByPos()` : 조회 결과의 컬럼 데이터를 위치(순번) 기준으로 저장

```c
    /*** 데이터 삽입 (INSERT) ***/
    char* insert_sql = "INSERT INTO TEST_CUSTOMER (CUSTOMER_ID, NAME, PHONE, BIRTH_DATE) VALUES (:1, :2, :3, :4)";
    OCIHandleAlloc(envhp, (void**)&stmthp, OCI_HTYPE_STMT, 0, NULL);
    OCIStmtPrepare(stmthp, errhp, (text*)insert_sql, strlen(insert_sql), OCI_NTV_SYNTAX,
        OCI_DEFAULT);
    int insert_id = 2;
    char insert_name[50] = "John";
    char insert_phone[50] = "010-5678-1234";
    char insert_birth_date[40] = "1397-03-13";
    
    OCIBind* bnd1 = NULL, * bnd2 = NULL, * bnd3 = NULL, * bnd4 = NULL;
    // 바인딩 변수 설정 (INSERT)
    OCIBindByPos(stmthp, &bnd1, errhp, 1, &insert_id, sizeof(insert_id), SQLT_INT, NULL,
        NULL, NULL, 0, NULL, OCI_DEFAULT);
    OCIBindByPos(stmthp, &bnd2, errhp, 2, insert_name, sizeof(insert_name), SQLT_STR,
        NULL, NULL, NULL, 0, NULL, OCI_DEFAULT);
    OCIBindByPos(stmthp, &bnd3, errhp, 3, insert_phone, sizeof(insert_phone), SQLT_STR,
        NULL, NULL, NULL, 0, NULL, OCI_DEFAULT);
    OCIBindByPos(stmthp, &bnd4, errhp, 4, insert_birth_date, sizeof(insert_birth_date), SQLT_STR,
        NULL, NULL, NULL, 0, NULL, OCI_DEFAULT);
    if (OCIStmtExecute(svchp, stmthp, errhp, 1, 0, NULL, NULL,
        OCI_COMMIT_ON_SUCCESS) != OCI_SUCCESS) {
        check_error(errhp);
    }
    else {
        printf("데이터 삽입 완료!\n");
    }
```
