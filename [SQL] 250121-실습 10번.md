# 실습과제 10

```sql
-- 누적은 시간을 기준으로
-- 거래 테이블에서 계좌별 누적 거래 금액을 계산하시오.
SELECT ACCOUNT_ID, AMOUNT, TRANSACTION_DATE,
    SUM(AMOUNT) OVER (PARTITION BY ACCOUNT_ID ORDER BY TRANSACTION_DATE) "누적 거래 금액"
FROM TRANSACTIONS;

-- 각 계좌별 거래 금액의 최고 거래 금액 대비 비율을 출력하시오. (AMOUNT / MAX AMOUNT)
SELECT ACCOUNT_ID, 
    TRUNC(AMOUNT / MAX(AMOUNT) OVER (PARTITION BY ACCOUNT_ID), 2) "최고 거래 금액 대비 비율"
FROM TRANSACTIONS;

-- 대출 테이블에서 지점별 누적 대출 금액을 계산하시오.
SELECT BRANCH_ID, LOAN_DATE,
    SUM(AMOUNT) OVER (PARTITION BY BRANCH_ID ORDER BY LOAN_DATE) "누적 대출 금액"
FROM LOANS;

-- 계좌 테이블에서 고객별 평균 잔액을 계산하시오. (반올림하여 소수점 둘째 자리까지 출력)
SELECT CUSTOMER_ID,
    TRUNC(AVG(BALANCE) OVER (PARTITION BY CUSTOMER_ID), 2) "평균 잔액"
FROM ACCOUNTS;
```
