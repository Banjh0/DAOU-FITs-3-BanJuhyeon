# TIL: String

학습일자: 2025년 02월 07일

## 학습하는데 어떤 문제에 부딪혔는지

여러 class에 대해 학습했다. 특히 String의 경우, 이를 사용한 경험은 있지만, 이론적으로 접근하는 법은 또 다르게 어려워서 학습을 요했다. 이에 String에 대해 배운 부분을 적어보려 한다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

강의를 열심히 들었다.

## 어떻게 그 문제를 해결 했는지

열심히 들은 강의를 정리했다.

## 오늘 내가 새롭게 배운 것은 무엇인지

일반적으로 인스턴스를 만들때는 new 라는 키워드를 통해 만든다. 그래야 Heap 영역에 해당 클래스에 대한 인스턴스가 만들어지고 생성자가 호출되고,  초기화가 이뤄진다.

특이하게도, 문자열은 클래스로 제공. 하지만, 이를 일일이 new를 통해서 객체로 만들면 Heap 메모리가 낭비되며 키보드를 치는 손가락 아프게 된다.

그래서 String에 한해서 assign을 통해 객체를 생성할 수 있다. 

→ `String a = “assign 가능한 클래스 String 입니다.”` 

조금 더 자세히 보자면, 객체가 생성되는 Heap 영역 내부에 String Pool 이라는 영역이 있다. 이에 assign을 통해 만들어지는 String은 immutable, 즉 불변 이라는 특성이 있어서 String Pool에 만들어지는데 이 때 기존의 문자열 그 자체가 바뀌는 것이 아닌, 새로운 문자열 객체가 만들어지고 새롭게 만들어지는 주소를 참조한다는 것이다.

이에 더해 같은 문자열의 경우, 효율을 위해 주소를 공유한다.

단, new 키워드를 통해서 만드는 문자열의 경우 Spring Pool이 아닌 Heap 영역에 새롭게 만들어 지는 것이다.
