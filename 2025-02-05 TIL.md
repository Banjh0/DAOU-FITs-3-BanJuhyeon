# TIL : setter와 getter

학습일자: 2025년 02월 05일

## 학습하는데 어떤 문제에 부딪혔는지

접근제어자에 대해서 학습을 했다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

강의를 열심히 들었다.

## 어떻게 그 문제를 해결 했는지

열심히 들은 강의를 정리했다.

## 오늘 내가 새롭게 배운 것은 무엇인지

### 접근 제어자 (Access Modifier)

총 4개(public, protected, default, private)

- `public` : **어디에서나 접근 가능**한 field, method, constructor, class 선언에 사용
- `protected` : 기본적으로 default와 같지만 상속관계에 있는 class에서 field, method, constructor를 사용할 수 있도록 선언
- `default` : **같은 package내**의 다른 class에서 접근 가능한 field, method, constructor, class 선언
`default`라는 키워드가 있지 않고, 다른 3가지를 안 쓰면 default가 되는 것.
- `private` : 다른 class에서는 접근할 수 없는 field, method, constructor 선언

특히 인상깊었던 점은 클래스를 만들 때 데이터, 즉 필드를 `private` 로 둬야지만 아무데서나 접근해서 수정하는 것을 막아야한다는 점이었다 “Information Hiding”. 

대신, getter와 setter을 이용해서 해당 값에 접근하거나 수정할 수 있었다. 이를 코드로 풀면,

```java
package aaa;

import bbb.*;

// 접근제어자 Access Modifier
public class PackageTest {

    // instance variable
    int k;

    // default constructor(기본생성자)
    public PackageTest() {

    }

    // entry point
    public static void main(String args[]) {
        MyClass a = new MyClass();
        a.printInfo();
        a.setBalance(200); // setter를 통한 외부 데이터 접근

    }
}

```

```java
package bbb;

public class MyClass {

    // 기본적으로 외부에서 Field에는 접근이 불가능하게 설정.
    private int balance;

    // constructor
    public MyClass() {

    }

    // method
    public void printInfo() {

    }

    // method 중
    // setter & getter => private field에 접근
    public int getBalance() {
        return this.balance;
    }

    public void setBalance(int balance) {
        this.balance = balance;
    }
}

```

이처럼 setter와 getter을 이용하는 것을 확인할 수 있었다.
