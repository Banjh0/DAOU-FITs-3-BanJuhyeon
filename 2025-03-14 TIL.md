# TIL: 구조체

학습일자: 2025년 03월 14일

## 학습하는데 어떤 문제에 부딪혔는지

구조체를 처음 배우게 되었다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

강의를 열심히 들었다.

## 어떻게 그 문제를 해결 했는지

강의를 열심히 듣고, 교재의 내용을 정리했다.

## 오늘 내가 새롭게 배운 것은 무엇인지

### 구조체

- 서로 다른 형태의 데이터를 여러 개 모아서 하나의 자료구조로 표현
- 구조체로 만들어진 자료는 연속된 메모리에 할당된다

### 구조체 선언 형식

```c
struct 구조체이름 {
	자료형 식별자1;
	자료형 식별자2;
	....
};
```

### 구조체 사용 방법

```c
struct 구조체이름 구조체변수;
```

```c
// 구조체 선언과 동시에 구조변수지정
struct 구조체이름 {
	자료형 식별자1;
	자료형 식별자2;
	...
} 구조체변수1, 구조체변수2;
```

```c
// typedef 이용하기
typedef struct st_score {
	int id;
	int kor;
	double avg;
} SCO;

SCO student1, student2; // 선언
```

### 구조체 초기화

```c
// 일괄 초기화
typedef struct st_score {
	int id;
	int kor;
	double avg;
} SCO;

SCO score1 = { 100, 100, 100.0 };
```

```c
// 구조체에 대해 멤버 단위로 초기화 수행
struct student { char name[20]; int kor; };

student st1;

strcpy(st1.name, "hee");
st1.kor = 100;
```
