# TIL: 포인터 연산과 응용

학습일자: 2025년 03월 12일

## 학습하는데 어떤 문제에 부딪혔는지

포인터를 배우고 있는데 아직도 어렵다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

강의를 열심히 들었는데도 어렵다.

## 어떻게 그 문제를 해결 했는지

사실 해결 못한 것 같기는 한데, 일단 정리를 해 보았다.

## 오늘 내가 새롭게 배운 것은 무엇인지

### 포인터 연산

C 언어에서 포인터는 변수의 메모리 주소를 가리키며, 이를 통해 직접 주소 값을 변경하거나 데이터를 수정할 수 있다.

### 주소 연산자 `&`

```
int score = 100;
int* pscore;
int id[5] = { 101, 204, 489, 328, 532 }; // 배열 [0] ~ [4]
int* pid;
int t = 1;
int* tp;

pscore = &score; // 일반 변수의 주소를 저장할 때는 & 사용
pid = id; // 배열명은 시작 주소이므로 & 없이 사용 가능
tp = &t; // 일반 변수는 주소연산자를 붙여야 함
```

### 포인터 연산 예제

포인터를 이용해 데이터를 변경하거나 이동할 수 있다.

```
printf("%d\n", ++ *pid); // 102 => *pid = *pid + 1 (101 + 1)
printf("%d\n", *++pid); // 204 => pid를 증가 후 *pid 값 출력
printf("%d\n", ++ *tp); // 2 => *tp = *tp + 1 (1 + 1)
printf("%d\n", *++tp); // 예상치 못한 값 출력 (메모리 주소 범위를 벗어날 가능성)
```

주의할 점은 일반 변수의 주소를 담은 포인터를 증감 연산할 경우, 의도하지 않은 메모리를 참조할 수 있다는 것이다.

### 포인터를 이용한 문자열 조작

C 언어에서 문자열은 `char` 배열로 저장되며, 포인터를 사용하여 효율적으로 다룰 수 있다.

```
char city1[] = "seoul"; // 스택 영역에 저장 (읽기/쓰기 가능)
char* city2 = "seoul";  // 데이터 영역에 저장 (주소 변경 가능)
```

### 문자열 변경

```
strcpy(city1, "busan"); // 배열 내 문자열 변경 가능
city2 = "busan"; // 포인터 변수의 주소 변경 가능
```

### 문자 출력 예제

```
printf("%c %c\n", city1[0], city2[0]);
printf("%c %c\n", *(city1 + 2), *(city2 + 2));
```

### 포인터를 이용한 문자열 조작

```
char city3[] = "seoul";
char* pcity3 = city3;
pcity3[0] = 'k'; // 배열 내용 변경 가능
printf("%s\n", pcity3);
```

주의할 점은 포인터 변수를 이용해 문자열을 출력할 때 `*pcity3`가 아니라 `pcity3` 자체를 출력해야 한다.

### 문자열 치환과 포인터 주소 변경

```
strcpy(pcity3, "busan"); // 문자열 치환 가능
printf("%s\n", pcity3);

pcity3 = "korea"; // 포인터 변수의 주소 변경 가능
printf("%s\n", pcity3);
```

이처럼 포인터를 활용하면 문자열을 유연하게 다룰 수 있다.
배열에서는 직접 수정이 가능하지만, 문자열 리터럴은 주소 변경만 가능하다는 점을 기억하자.
