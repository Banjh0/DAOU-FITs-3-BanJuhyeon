# TIL: JAVA 실행 순서 확인하기

학습일자: 2025년 02월 06일

## 학습하는데 어떤 문제에 부딪혔는지

강의 중 배운 내용을 토대로 JAVA 코드를 실행하면 출력되는 내용에 대한 부분을 확인했는데, 예상과는 달랐다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

다시 작동 순서를 상기했지만, 한 부분 이해가 되지 않는 부분이 있었다.

## 어떻게 그 문제를 해결 했는지

이해가 되지 않았던 부분은 강사님의 설명을 통해 이해할 수 있었다.

## 오늘 내가 새롭게 배운 것은 무엇인지

```java
public class SuperClass {

    static int staticCall(String msg) {
        System.out.println(msg);
        return 0;
    }

    int a = staticCall("1번 문장입니다.");

    static int b = staticCall("2번 문장입니다.");

    public SuperClass() {
        System.out.println("3번 문장입니다.");
    }

    public SuperClass(int i )
        this();
        staticCall("4번 문장입니다.");
    }

    public void myFunc() {
        System.out.println("5번 문장입니다.");
    }

}

```

```java
public class Exam01_InheritanceOrder extends SuperClass {
    int c = staticCall("6번 문장입니다.");
    static int d = staticCall("7번 문장입니다.");

    public Exam01_InheritanceOrder() {
        super(100);
        staticCall("8번 문장입니다.");
        super.myFunc();
    }

    @Override
    public void myFunc() {
        System.out.println("9번 문장입니다.");
    }

    public static void main(String[] args) {
        System.out.println("10번 문장입니다.");
        SuperClass obj = new Exam01_InheritanceOrder();
        obj.myFunc();
    }
}

```

위 코드에서, 실제 작동 순서는 2 → 7 → 10 → 1 **→** 3 → 4 → 6 → 8 → 5 → 9이다.

- 클래스에 대한 정보가 메소드 영역에 올라가야 하는데, 상위 클래스를 상속받은 경우 상위 클래스의 정보가 먼저 올라간다.
- 따라서 상위 클래스 중 static에 해당되는 2가 출력된다
- 이후 main 메소드가 있는 클래스로 돌아와 클래스 정보를 읽는데, static인 8이 출력된다.
- main 메소드가 실행되어 10이 출력되고,
- 이후 인스턴스를 생성하러 간다.
- 인스턴스를 생성할 때 상위 클래스부터 1, 3, 4가 출력되고
- 이후 하위 클래스의 6이 출력된다.
- 이렇게 인스턴스의 필드가 만들어지면 8이 출력되고,
- 상위 클래스의 메소드인 5가 출력된다.
- 마지막이 헷갈렸던 부분인데, obj의 myFunc() 메소드는 동적 바인딩에 의해 9가 출력된다.
