# TIL: 커밋된 데이터 복구 방법

학습일자: 2025년 01월 24일

## 학습하는데 어떤 문제에 부딪혔는지

추후 현업에서 DB를 만지게 될 때 가장 문제가 잘못된 부분을 커밋하는 것일텐데 걱정이 되었다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

일단 최대한 문제를 만들지 않기 위해서 열심히 공부를 했다.

## 어떻게 그 문제를 해결 했는지

강사님이 알려주신 커밋된 데이터 복구방법을 정리했다.

## 오늘 내가 새롭게 배운 것은 무엇인지

### * COMMIT된 데이터 복구 방법 *

- 플래시백 쿼리 (Flashback Query)
    - 오라클 데이터베이스는 UNDO 테이블스페이스를 사용하여 과거 데이터 조회 가능
    - 플래시백 쿼리는 특정 시점의 데이터를 복구하는데 유용
- AS OF TIMESTAMP 절을 사용하여 5분 전 데이터를 조회
- 이 데이터를 기반으로 복구 작업 수행
    
    ```sql
    -- 특정 시점의 데이터를 조회
    SELECT *
    FROM employees
    AS OF TIMESTAMP (SYSTIMESTAMP - INTERVAL '5' MINUTE);
    ```
    

### 플래시백을 이용한 복구의 전제 조건

- 설정된 보존 기간(UNDO_RETENTION) 안에 복구 해야함
- UNDO_RETENTION 확인 쿼리
    
    ```sql
    -- UNDO_RETENTION 설정 값 확인
    SELECT name, value
    FROM v$parameter
    WHERE name = 'undo_retention';
    ```
    

### 플래시백 활용 사례

- 실수로 삭제된 데이터 복구
    
    ```sql
    -- 데이터 삭제
    DELETE FROM employees;
    COMMIT;
    
    -- 삭제된 데이터 복구
    INSERT INTO employees
    SELECT *
    FROM employees
    AS OF TIMESTAMP (SYSTIMESTAMP - INTERVAL '5' MINUTE);
    ```
