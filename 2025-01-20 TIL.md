# TIL: 서브쿼리

학습일자: 2025년 01월 20일

## 학습하는데 어떤 문제에 부딪혔는지

실습 문제를 푸는데 있어 어려움이 많이 느껴졌다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

개념정리를 다시하고 문제를 다시 풀어보고자 했다.

## 어떻게 그 문제를 해결 했는지

강의를 다시 열심히 듣고, 배운 내용을 복습했다.

## 오늘 내가 새롭게 배운 것은 무엇인지

### 서브쿼리 (Subquery)

- SQL문 안에 포함된 또 다른 SQL문
- 메인 쿼리에 값을 제공하거나 조건을 지정하는 역할
- 때에 따라서 메인 쿼리 안에 서브쿼리가 여러개 있을 수 있음.
- 일반적으로 괄호로 감싸져 있으며, SELECT, INSERT, UPDATE, DELETE 문에서 사용 가능

### 서브쿼리 분류

- (메인쿼리와)연관 여부에 따른 분류: 비연관 서브쿼리, 연관 서브쿼리
- 위치에 따른 분류: 스칼라 서브쿼리, 인라인 뷰, 중첩 서브쿼리
- 반환값 유형에 따른 분류: 단일 행 서브쿼리, 다중행 서브쿼리, 다중열 서브쿼리

### 비연관 서브쿼리 (Non-correlated Subquery)

- 서브쿼리가 독립적으로 실행되어 결과를 반환
- 메인 쿼리와 서브쿼리 간 상호작용 없음
- 예시
    
    ```sql
    -- 평균 급여보다 높은 급여를 받는 직원 조회
    SELECT NAME
    FROM EMPLOYEES
    WHERE SALARY > (SELECT AVG(SALARY) FROM EMPLOYEES);
    -- 비연관 AND 단일행 AND 단일열 서브쿼리.
    -- 다중연산자 IN, EXIST, SOME, ANY, ALL... 등은 단일 / 다중 연산자 모두 사용 가능.
    ```
    

### 연관 서브쿼리 (Correlated Subquery)

- 서브쿼리가 메인 쿼리의 각 행마다 실행
- 성능적으로 비연관 서브쿼리보다 살짝 무거움.
- 메인 쿼리의 데이터를 서브쿼리에서 참조
- 예시
    
    ```sql
    -- 동일한 부서에서 급여가 가장 높은 직언 조회
    SELECT NAME FROM EMPLOYEES E1
    WHERE SALARY = (
    		SELECT MAX(SALARY)
    		FROM EMPLOYEES E2
    		WHERE E1.DEPARTMENT_ID = E2.DEPARTMENT_ID);
    ```
    

### 스칼라 서브쿼리 (Scalar Subquery)

- 서브쿼리가 컬럼 값으로 반환되어 SELECT 절에서 사용
- 주로 집계 값이나 계산 결과를 포함할 때 활용
- JOIN으로 활용하기에는 JOIN이 너무 복잡한 경우, 혹은 공통쿼리 테이블로도 활용됨.
- 예시
    
    ```sql
    -- 각 직원의 급여와 해당 부서의 평균 급여를 조회
    SELECT NAME, SALARY,
    		(SELECT AVG(SALARY) FROM EMPLOYEES
    		WHERE DEPARTMENT_ID = E.DEPARTMENT_ID) AS AVG_SALARY
    FROM EMPLOYEES E;
    
    -- 스칼라 서브쿼리는 독특하게 캐싱기능이 존재. 값의 종류가 적은 것들(성별)을 활용하고 싶을 때 스칼라서브쿼리를 이용하면 속도 향상.
    ```
    

### 인라인 뷰 (Inline View)

- FROM 절에서 서브쿼리 결과를 테이블처럼 사용
- 별칭을 붙여 활용 가능
- 예시
    
    ```sql
    -- 부서별 평균 급여를 계산하고 각 부서의 이름과 평균 급여를 조회
    SELECT D.DEPARTMENT_NAME, T.AVG_SALARY
    FROM DEPARTMENTS D,
    		(SELECT DEPARTMENT_ID, AVG(SALARY) AS AVG_SALARY
    		FROM EMPLOYEES GROUP BY DEPARTMENT_ID) T
    WHERE D.DEPARTMENT_ID = T.DEPARTMENT_ID;
    
    --GROUP BY가 인라인 뷰 안에 있음. 그룹핑을 먼저 하고 JOIN 하므로 JOIN할 row들이 적어질 수 있음. 그렇지만, 반드시 빠르다고 할 수 는 없음.
    ```
    

### 중첩 서브쿼리 (Nested Subquery)

- WHERE 절에서 조건을 설정하기 위해 서브쿼리 사용
- → 가장 많이 사용되는 서브쿼리.
- 서브쿼리 결과에 따라 메인 쿼리의 필터링 결정
- 예시
    
    ```sql
    -- 특정 부서에 속한 직원 조회
    SELECT NAME
    FROM EMPLOYEES
    WHERE DEPARTMENT_ID = (SELECT DEPARTMENT_ID
    												FROM DEPARTMENTS
    												WHERE DEPARTMENT_NAME = 'Department 1');
    ```
    
- HAVING 절에서 조건을 설정하기 위해 집계 함수와 함께 활용
- 예시
    
    ```sql
    -- 평균 급여 이상이 부서의 총 급여 조회
    SELECT DEPARTMENT_ID, SUM(SALARY) AS TOTAL_SALARY
    FROM EMPLOYEES
    GROUP BY DEPARTMENT_ID
    HAVING SUM(SALARY) > (SELECT AVG(SUM(SALARY))
    											FROM EMPLOYEES
    											GROUP BY DEPARTMENT_ID);
    ```
    

### 단일 행 서브쿼리 (Single-row Subquery)

- 서브쿼리가 단일 행과 단일 열을 반환
- 스칼라 서브쿼리는 컬럼으로 사용하기 때문에 무조건 단일 행 서브쿼리로 반환되어야 함, 안그러면 에러남. → 하나의 값만 나타나도록 보장되게끔 작성해야 함.
- 비교 연산자(=, <, >,… 등)와 함께 사용
- 예시
    
    ```sql
    -- 가장 높은 급여를 받는 직원 조회
    SELECT NAME
    FROM EMPLOYEES
    WHERE SALARY = (SELECT MAX(SALARY)
                    FROM EMPLOYEES);
    -- 여기서 MAX가 단일임을 보장함.
    -- 만약, 보통은 단일이지만 두 줄이 나올수도 있을 것 같은 경우에는 WHERE절에 ROWNUM = 1을 붙여 단위를 보장하는 경우도 있음.
    ```
    

### 다중 행 서브쿼리 (Multi-row Subquery)

- 서브쿼리가 여러 행을 반환
- 다중 행 연산자(IN, ANY, ALL)와 함께 사용
- 예시
    
    ```sql
    -- 특정 부서들에 속한 직원 조회
    SELECT NAME
    FROM EMPLOYEES
    WHERE DEPARTMENT_ID IN (SELECT DEPARTMENT_ID
                            FROM DEPARTMENTS
                            WHERE LOCATION = 'New York');
                            
    -- 지금 상태에서는 하나일 수 있지만, 뉴욕에 여러 부서가 생기면 하나임을 보장할 수 없음.
    ```
    

### 다중 열 서브쿼리 (Multi-column Subquery)

- 서브쿼리가 여러 열을 반환
- 메인 쿼리의 여러 열과 비교 가능
- 예시
    
    ```sql
    -- 특정 부서와 직무를 가진 직원 조회
    SELECT NAME
    FROM EMPLOYEES
    WHERE (DEPARTMENT_ID, JOB_ID) IN (
            SELECT DEPARTMENT_ID, JOB_ID
            FROM JOB_HISTORY);
            
    ```
    

### 서브쿼리 사용 시 유의사항

- 성능 최적화
    - 비연산 서브쿼리는 독립적으로 실행되므로 더 빠름
    - 연관 서브쿼리는 메인 쿼리의 각 행마다 실행되므로 성능 저하 가능
- 적절한 반환값 확인
    - 서브쿼리가 반환하는 값의 유형(단일 값, 다중 값)에 따라 연산자 선택이 중요
- JOIN 대체 가능성
    - 일부 서브쿼리는 JOIN으로 대체할 수 있어 성능 개선 가능
    - 그때 그때 다름.
