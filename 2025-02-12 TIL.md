# TIL: MVC 패턴

학습일자: 2025년 02월 12일

## 학습하는데 어떤 문제에 부딪혔는지

MVC 패턴의 구조에 대해 학습했는데, 개념이 알 듯 하면서도 생소했다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

강의를 열심히 들었다.

## 어떻게 그 문제를 해결 했는지

강의를 정리하고, 복습했다.

## 오늘 내가 새롭게 배운 것은 무엇인지

### MVC 패턴

- Model

(데이터 모델, 비즈니스 로직 모델(= 서비스 모델) 2가지)

데이터 모델은 프로그램에서 사용하는 클래스, 파생되는 인스턴스를 의미한다. 일반적으로 데이터베이스를 끼고 있는 프로그램은 DB 안의 데이터를 가지고 VO를 만든다. *VO*가 데이터 모델에 해당된다.

서비스 모델은 *로직처리하는 객체,* 그리고 그 객체를 만들기 위한 클래스를 의미한다.

- View

프레젠테이션 레이어처럼 *입출력을 담당한다*. *(로직처리 X)*

JavaFX에서 stage와 xml이 여기에 해당. 

- Controller

*View와 Service 연결.* 

뷰로 부터 입력받아, 서비스모델에 있는 로직에 연결하는 중간 매개체 역할을 맡는다.

그래서 이 안에 이벤트 처리가 같이 들어감. JavaFX에서 이벤트 처리하는 클래스를 Controller라고 부르는 이유다.

그럼 데이터베이스 처리는? → DAO를 따로 만든다.

- DAO (Data Access Object)

→ *Database 처리*

즉, 로직처리하는 곳에서는 DB 처리 하지 않는다는 것.

그래서 서비스가 DAO를 가져와 DB처리를 한다. 

기억해야 할 것은 서비스 모델에서 로직처리를 하면서 Transaction을 담당. DAO에서는 로직이 나오지 않고, 로직은 Transaction을 포함한 서비스에서 나온다. 즉, DAO에서는 (비즈니스)*로직처리가 나오면 안되고* *단순 DB Access처리만 허용된다.*
