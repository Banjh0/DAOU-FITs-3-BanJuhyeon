# TIL: 포인터를 이용한 구조체

학습일자: 2025년 03월 17일

## 학습하는데 어떤 문제에 부딪혔는지

구조체를 공부하는데 거기에 포인터를 이용하는 방법을 배우기 시작했다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

강의를 열심히 들었다.

## 어떻게 그 문제를 해결 했는지

강의를 열심히 듣고, 정리했다.

## 오늘 내가 새롭게 배운 것은 무엇인지

### 포인터를 이용한 구조체와 아닌 구조체의 차이

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

typedef struct student {
	char name[20];
	char sex;
	int stid;
	int sub1;
	int sub2;
	int sub3;
	double avg;
} ST;

int input(ST st);

int main() {

	ST st1 = { "kdhong", 'm', 1508001, 0, 0, 0, 0.0 };
	input(st1);
	printf("main() 함수에서 출력된 세 과목의 점수는 %d, %d, %d입니다.\n", st1.sub1, st1.sub2, st1.sub3);

	return 0;
}

int input(ST st) {
	printf("%s 학생의 3과목 성적을 입력하세요(공란으로 구분) : ", st.name);
	scanf("%d %d %d", &st.sub1, &st.sub2, &st.sub3);
	printf("input() 함수에서 입력된 값은 %d, %d, %d입니다.\n", st.sub1, st.sub2, st.sub3);
}
```

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

typedef struct student {
	char name[20];
	char sex;
	int stid;
	int sub1;
	int sub2;
	int sub3;
	double avg;
} ST;

int input(ST* st);

int main() {
	int i;

	ST st[3] = { {"kdhong", 'm', 1508001, 0, 0, 0, 0.0}, {"yhkim", "f", 1608021, 0, 0, 0, 0.0}, {"cskim", 'm', 1608026, 0, 0, 0, 0.0} };
	for (i = 0; i < 3; i++) {
		input(&st[i]);
		printf("main() 함수에서 출력된 세 과목의 점수는 %d, %d, %d입니다.\n", st[i].sub1, st[i].sub2, st[i].sub3);
	}
	

	return 0;
}

int input(ST *st) {
	printf("%s 학생의 3과목 성적을 입력하세요(공란으로 구분) : ", st->name);
	scanf("%d %d %d", &st->sub1, &st->sub2, &st->sub3);
	printf("input() 함수에서 입력된 값은 %d, %d, %d입니다.\n", st->sub1, st->sub2, st->sub3);
}
```

- 위 두 실습과제를 보면, 전자의 경우 **변수** ST st가 인자로 넘어가지만, 원본 그 자체가 넘어간 것이 아니기 때문에(지역변수니까) input() 함수에서는 입력한 값이 제대로 출력되지만 main() 함수에서는 다 초기값인 0으로 출력됨을 알 수 있다.
- 반면, 후자는 **포인터 변수** ST의 구조체 포인터로 인자를 넘기기 때문에 input() 함수와 main() 함수 모두에서 입력한 값이 출력되는 것을 확인할 수 있다.
- 또한, 일반 구조체의 멤버 접근 연산자는 `.` (마침표) 연산자를 활용하고,
- 구조체 포인터를 사용한 멤버의 접근에는 `->` (화살표) 연산자를 활용함을 확인할 수 있다.
