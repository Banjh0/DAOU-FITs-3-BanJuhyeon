# TIL: Oracle DB PK 데이터 타입 변환하기

학습일자: 2025년 02월 25일

## 학습하는데 어떤 문제에 부딪혔는지

DB 테이블을 수정하는 도중, ALTER를 통해 곧바로 기존 ID값의 데이터를 변경할 수 없었다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

ALTER문에 대해 다시 학습하고 적용하려 했지만, 성공하지 못했다.

## 어떻게 그 문제를 해결 했는지

직접적으로 VARCHAR2 타입을 NUMBER로 바꾸기는 어렵다고 한다. 이에 단계별 작용을 통해 해당 부분을 수정할 수 있었다

## 오늘 내가 새롭게 배운 것은 무엇인지

### **1. SEQUENCE 생성**

```sql
CREATE SEQUENCE users_seq START WITH 1 INCREMENT BY 1 NOCACHE;
```

- 우선, 새로운 `NUMBER` 타입의 `userId` 값을 자동 증가하도록 시퀀스를 생성한다.

### **2. 기존 테이블에 새로운 컬럼 추가**

```sql
ALTER TABLE users ADD userId_new NUMBER(20);
```

- `NUMBER` 타입의 새로운 컬럼(`userId_new`)을 추가한다.

### **3. 기존 데이터에 새로운 ID 값 채우기**

```sql
UPDATE users SET userId_new = users_seq.NEXTVAL;
```

- 기존 행에 새로운 `userId_new` 값을 채운다.

### **4. 기존 PK 제약 조건 제거**

```sql
ALTER TABLE users DROP CONSTRAINT pk_users;
```

- 기존 `VARCHAR2(64)` 타입의 `userId`가 PK라면 삭제하는 과정을 추가한다.

### **5. 기존 컬럼 삭제**

```sql
ALTER TABLE users DROP COLUMN userId;
```

- `VARCHAR2(64)` 타입의 `userId`를 삭제한다.

### **6. 새로운 컬럼 이름 변경**

```sql
ALTER TABLE users RENAME COLUMN userId_new TO userId;
```

- 새 `NUMBER` 타입 컬럼을 기존 컬럼 이름으로 변경.

### **7. 새로운 컬럼을 PK로 설정**

```sql
ALTER TABLE users ADD CONSTRAINT pk_users PRIMARY KEY (userId);
```

- 새 `userId`를 PK로 설정한다.
