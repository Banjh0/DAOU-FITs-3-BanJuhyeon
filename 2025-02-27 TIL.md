# TIL: SQL 복습 2

학습일자: 2025년 02월 27일

## 학습하는데 어떤 문제에 부딪혔는지

어제에 이어서 SQL JOIN 관련을 복습했다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

이전에 배운 내용을 먼저 생각해봤다.

## 어떻게 그 문제를 해결 했는지

필기 내용을 복습했다.

## 오늘 내가 새롭게 배운 것은 무엇인지

### RIGHT OUTER JOIN

- RIGHT 테이블의 모든 행과 일치하는 LEFT 테이블의 데이터를 반환
- 일치하지 않는 경우 NULL 반환
- 예시
    
    ```sql
    SELECT E.NAME, D.DEPARTMENT_NAME
    FROM EMPLOYEES E
        RIGHT OUTER JOIN DEPARTMENTS D
            ON E.DEPARTMENT_ID = D.DEPARTMENT_ID    
    >> 부서가 기준
    ```
    

### FULL OUTER JOIN

- LEFT 테이블과 RIGHT 테이블의 모든 행 반환
- 일치 하지 않는 데이터는 NULL로 표시
- 예시
    
    ```sql
    SELECT E.NAME, D.DEPARTMENT_NAME
    FROM EMPLOYEES E
        FULL OUTER JOIN DEPARTMENTS D
            ON E.DEPARTMENT_ID = D.DEPARTMENT_ID
    >> 합집합
    ```
    

### SELF JOIN

- 같은 테이블을 두 번 사용하여 JOIN 수행 → 내가 나를 JOIN
- 주로 계층 구조를 표현할 때 사용
- 예시
    
    ```sql
    SELECT E1.NAME AS EMPLOYEES, E2.NAME AS MANAGER
    FROM EMPLOYEES E1
        JOIN EMPLOYEES E2
            ON E1.MANAGER_ID = E2.EMPLOYEE_ID;
    ```
    

### CROSS JOIN

- 두 테이블 간의 모든 행의 조합을 반환 (Cartesian Product)
- JOIN 조건이 없는 경우 발생
- 결과 집합의 크기가 매우 커질 수 있으므로 조심해서 사용
- 예시
    
    ```sql
    SELECT E.NAME, D.DEPARTMENT_NAME
    FROM EMPLOYEES E
    CROSS JOIN DEPARTMENTS D;
    ```
    

### NATURAL JOIN

- 동일한 이름의 컬럼을 기준으로 JOIN
- 조건을 명시하지 않아도 자동으로 연결
- EQUAL 조건으로 연결됨
- DEPARTMENT_ID가 동일하게 존재한다는 전제.
- 예시
    
    ```sql
    SELECT NAME, DEPARTMENT_NAME
    FROM EMPLOYEES
    NATURAL JOIN DEPARTMENTS;
    ```
