# TIL: 함수 포인터

학습일자: 2025년 03월 13일

## 학습하는데 어떤 문제에 부딪혔는지

어제 배운 함수 포인터에 대한 내용을 틀렸다.

## 그 문제를 해결하기 위해 내가 시도 해본 것은

함수 포인터 관련된 부분을 다시 읽었다.

## 어떻게 그 문제를 해결 했는지

읽고 정리했다.

## 오늘 내가 새롭게 배운 것은 무엇인지

### 함수포인터

함수포인터란, 함수의 시작주소를 변수에 저장하여 사용하는 것이다.

- 프로그램은 일반적으로 컴파일러에 의해 함수 단위의 실행 파일로 만들어지는데,
- 하나의 프로그램에 여러 개의 함수가 존재할 경우 여러 개의 실행 파일이 만들어지며 이 경우 여러 개의 실행 파일들은 시작주소에 의해 구분된다.
- 함수 시작주소를 포인터 변수에 저장하여 사용하는 방법을 함수 포인터라 한다.
- 함수 포인터는 같은 형태의 함수가 여러 개 있을 때 하나의 함수 포인터로 여러 개의 함수를 호출하여 사용할 경우 유용하다.

“하나의 프로그램에 여러 개의 함수가 존재할 경우 여러 개의 실행파일이 만들어지며, 이 경우 여러 개의 실행파일들은 시작 주소에 의해 구분된다.”  라는 부분에 실행 파일이 .exe 파일이니까 당연히 솔루션별로 .exe 파일은 하나 만들어지므로 해당 문항이 틀린 보기라고 생각했으나, ①번 보기가 “함수 포인터는 **서로 다른 형식의 함수**가 여러개 있을 때 하나의 함수 포인터로 여러개의 함수를 호출하여 사용할 경우 유용하다.” 라고 되어있어서 정확하게 틀린 보기임을 알 수 있었다.

생각해봐도, `int (*f)(int, int);` 라고 함수포인터를 선언했다는 것 자체가, 동일한 인자를 받는 함수에만 사용할 수 있다는 것이므로 서로 다른 형식의 함수가 대상이 될 수 없다는 것을 알 수 있었다.

특히 본인이 잘못 고른 항목에서의 “실행파일”이라는 의미는 .exe파일이 아닌 기계어 실행파일인 .s을 의미하는데, 이는 따로 저장되지 않는다.
